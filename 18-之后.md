# 18
## 1
你好！很高兴再次见到你。

这次的课件（Lec 18）看起来更像是一个**“高阶综合与实战应用”**的讲座。它不再纠结于死板的定义，而是直接把统计学抛到了真实的生物医学场景中——从疫苗研发到基因组学，再到经典的孟德尔遗传。

我们将这份课件的前半部分（Slide 1-14）提炼为三个核心模块。这部分内容的重点在于：**如何正确地报告数据**、**如何估计参数（MLE）**，以及**如何避免数据中的陷阱（辛普森悖论）**。

---

### 第一模块：科研的“规矩”——数据报告标准
**(Slide 3)**

这页PPT放了一张 *Nature* 期刊的 Reporting Checklist，这不仅仅是一张图，这是你未来职业生涯的“保命符”。

*   **核心要点**：
    1.  **样本量 (n)**：必须精确。不能只说“做了一批老鼠”，要说是 n=6 还是 n=100。
    2.  **描述统计**：你用了均值（Mean）还是中位数（Median）？如果数据是正态分布用均值，偏态分布（如收入、住院天数）通常用中位数。
    3.  **误差棒 (Error bars)**：这是最容易混淆的地方！
        *   **SD (标准差)**：描述数据的**离散程度**（这群人高矮差别大不大）。
        *   **SEM (标准误)**：描述**均值的可靠程度**（如果重做实验，均值会在哪里波动）。
        *   *教授提示*：如果你想展示数据本身，用SD；如果你想比较两组差异（做t检验），通常用SEM。

---

### 第二模块：参数估计的基石——极大似然估计 (MLE)
**(Slide 5-6)**

这是统计学中最“硬核”的理论基础之一。Slide 5 用新冠疫苗（Covaxin）试验为例，Slide 6 给出了数学推导。

*   **场景**：
    *   疫苗组 12221人，感染24人。
    *   安慰剂组 12198人，感染106人。
    *   我们要算感染率（Attack Rate, AR）。
*   **什么是 MLE (Maximum Likelihood Estimation)？**
    *   直觉理解：**“既然事情已经发生了（比如掷硬币出了正面），那么硬币正面朝上的概率 $\pi$ 应该是多少，才能让这件事发生的可能性最大？”**
*   **推导逻辑 (Slide 6)**：
    1.  建立模型：感染服从**二项分布**（Binomial）。
    2.  写出似然函数 $L(\pi|D)$。
    3.  求导数并令其为0（寻找极值点）。
    4.  **结论**：$\hat{\pi} = k/n$。
    *   *教授划重点*：你可能觉得 $\hat{\pi} = 24/12221$ 是废话，这不就是频率吗？但在统计学上，这证明了**频率是概率的极大似然估计**。这是所有后续复杂模型（如Logistic回归）参数估计的源头。

---

### 第三模块：数据的陷阱——辛普森悖论 (Simpson's Paradox)
**(Slide 8-10)**

这是前半部分最精彩、也是最容易在考试和临床分析中栽跟头的地方。请务必盯着 **Slide 9 (Hospital A vs B)** 和 **Slide 10 (China vs Italy)**。

**1. 医院案例 (Slide 9)**
*   **表象**：
    *   A医院总死亡率：16.6% (30/180)
    *   B医院总死亡率：13.2% (39/295)
    *   *初学者结论*：B医院水平高，大家去B医院。
*   **真相（分层后）**：
    *   轻症（Mild）：A (2.5%) **优于** B (5%)
    *   中症（Moderate）：A (16.6%) **优于** B (20%)
    *   重症（Severe）：A (45%) **优于** B (67%)
    *   *教授发问*：**为什么A医院每个单独的组都比B好，但加起来却比B差？**
*   **核心解释**：**混杂因素（Confounding Factor）——病情的严重程度**。
    *   A医院收治了大量重症患者（40/180），而B医院绝大多数是轻症（180/295）。
    *   **不同质**！我们在上一节课讲过，比较的前提是“同质”。

**2. 新冠病死率案例 (Slide 10)**
*   同理，意大利的总病死率（4.45%）看起来比中国（2.31%）高很多。
*   但分年龄段看（0-19, 20-39...），两者的差异变小了。
*   **原因**：意大利是老龄化国家，高龄组（死亡高风险人群）占比极大。

**3. 教授的教诲**
*   当看到两个总体比较出现反直觉的结果时，第一反应要是：**“有没有混杂因素？是不是样本构成不同质？”**
*   **解决方法**：**分层分析 (Stratification)** 或者 **标准化 (Standardization)**。不要被总体的平均数骗了！

---

### 第四模块：生物统计的背景——寻找信号
**(Slide 11-12)**

这部分展示了统计学在基因组学中的应用。

*   **CpG Island (Slide 12)**：
    *   人类基因组中 GC 含量的期望是 4.4%（基于 42% GC含量的随机组合）。
    *   但实际上我们观察到了 CpG 聚集区（CpG Islands）。
    *   *统计意义*：**实际观测值 $\gg$ 期望值**。这说明这些序列不是随机出现的，而是有生物学功能的（比如启动子区域）。这是统计检验（如卡方检验）的生物学基础。

---

### 前半部分复习总结

同学，这14页PPT看似杂乱，其实在传达三个理念：

1.  **严谨性**：做科研，Checklist不能少，n和Error bar要标清。
2.  **理论性**：我们算出来的每一个“率”，背后都有 MLE 在做数学支撑。
3.  **批判性（最重要）**：看到 Hospital A 死亡率高，不要急着骂医生，先看看是不是他们收的重病人多（辛普森悖论）。

接下来的 Slide 15 开始，我们将进入**孟德尔的豌豆实验**，那里将引出著名的**卡方检验**和**拟合优度**问题。准备好了吗？我们继续！


## 2
你好！我们继续。

如果说前半部分是在教你“如何正确看数据”，那么这后半部分（Slide 15-30）则是在教你**“如何审视数据的真实性”**以及**“统计方法选择的哲学”**。

这部分内容非常精彩，因为它挑战了两个权威：一个是遗传学之父**孟德尔**（数据可能造假？），一个是我们的直觉（同样的实验结果，P值竟然会不同？）。

我们将这部分划分为三个核心模块：**孟德尔数据的争议（拟合优度检验）**、**安斯库姆四重奏（可视化的重要性）**，以及**假设检验中的“停止规则”**。

---

### 第五模块：当数据“太完美”时——孟德尔与卡方检验
**(Slide 15-24)**

这是一个在统计学界著名的“公案”。

**1. 拟合优度检验 (Goodness of Fit) —— Slide 17-18**
*   **背景**：孟德尔做豌豆杂交，理论上 $F_2$ 代的显性:隐性应该是 **3:1**。
*   **计算**：他观察到了 7324:5474（圆粒:皱粒），理论上应该是 5474:1850。
*   **工具**：**卡方检验 ($\chi^2$ Test)**。
    *   公式：$\chi^2 = \sum \frac{(O-E)^2}{E}$ （观测值减期望值的平方，除以期望值）。
    *   **注意**：这里的用法和通常比较两组差异（如吃药vs不吃药）不同。这里我们**希望 P 值越大越好**。
    *   *逻辑*：
        *   差异检验：希望 $P < 0.05$，证明有区别。
        *   拟合优度：希望 $P > 0.05$，证明**数据符合理论模型**（也就是差异是随机误差导致的）。
*   **结果**：Slide 18 显示 $P = 0.61$，说明数据和 3:1 的理论**吻合得很好**。

**2. 费雪的质疑 (Fisher's Critique) —— Slide 19-24**
*   **问题出在哪？**
    *   单个实验 $P=0.61$ 很正常。
    *   但是，当你把孟德尔的所有实验加在一起（Slide 20），总的 $\chi^2$ 统计量对应的 P 值竟然高达 **0.99993**！
*   **教授划重点（必考思维）**：
    *   **$P \approx 1$ 是极度可疑的。** 真实的随机实验总会有误差，数据应该在理论值左右波动。P值如果在 0-1 之间均匀分布才是正常的（Slide 24）。
    *   **结论**：统计学大师 R.A. Fisher 认为孟德尔（或者他的“匿名助手”）可能**修改了数据**，剔除了一些“长得不像3:1”的豌豆，或者存在**确认偏误 (Confirmation Bias)**。
    *   **教训**：数据太完美往往意味着造假或人为筛选。

---

### 第六模块：不要只看平均数——安斯库姆四重奏
**(Slide 25-26)**

**Slide 26** 展示了四组数据，被称为 **Anscombe's Quartet**。

*   **陷阱**：这四组数据（I, II, III, IV）的统计指标**完全一模一样**！
    *   平均数 ($\bar{x}, \bar{y}$) 相同。
    *   方差 ($\sigma^2$) 相同。
    *   相关系数 ($R^2$) 都是 0.67。
    *   回归方程都是 $y = 3 + 0.5x$。
*   **真相**：看图！
    *   图1：正常的线性关系。
    *   图2：明显的曲线关系（非线性）。
    *   图3：有一个明显的离群点（Outlier）拉高了回归线。
    *   图4：完全没有关系，全靠一个离群点硬生生造出了相关性。
*   **教授建议**：**作图（Data Presentation）是分析的第一步**。永远不要只相信 Excel 算出来的平均数和 P 值，一定要把数据画出来看一眼分布。

---

### 第七模块：实验设计决定 P 值——二项分布 vs 负二项分布
**(Slide 27-29)**

这是这节课最烧脑、也最能体现统计学深度的部分。

**1. 案例背景 (Slide 27)**
*   药物A（老药）有效率 $\pi = 0.5$。
*   药物B（新药）做了实验：12只老鼠，9只有效，3只无效。
*   问题：药物B是否比A更好（$\pi > 0.5$）？

**2. 两种不同的模型与结论**

*   **模型一：二项分布 (Binomial) —— Slide 28**
    *   **假设**：我们事先定好了**“做12次实验”** ($n=12$ 是固定的)。
    *   **计算**：在 $n=12$ 的情况下，出现 $\ge 9$ 次成功的概率是多少？
    *   **结果**：$P = 0.075$。
    *   **结论**：$P > 0.05$，**不拒绝 $H_0$**。新药没证明比老药好。

*   **模型二：负二项分布 (Negative Binomial) —— Slide 29**
    *   **假设**：我们事先定好了**“做到出现3次无效为止”** ($k=3$ 是固定的，而总次数 $n$ 是随机的)。
    *   **计算**：在第12次实验时恰好凑齐3次失败的概率。
    *   **结果**：$P = 0.033$。
    *   **结论**：$P < 0.05$，**拒绝 $H_0$**。新药比老药好！

**3. 教授深层解析（难点）**
*   为什么同样的数据（9好3坏），P值不同？
*   因为**P值的定义依赖于“比当前观测更极端的数据空间”**。
    *   **二项分布**考虑的是：如果我再做一次实验（n=12），出现 9, 10, 11, 12 次成功的概率。
    *   **负二项分布**考虑的是：如果我一直做实验直到3次失败，可能总共只需要 3, 4, ... 11 次就能结束的概率。
*   **教训**：**停止规则 (Stopping Rule)** 必须在实验开始前这就定好！你不能做着做着看数据不错就停下来（这叫 P-hacking），那样算出来的 P 值是错的。

---

### 第八模块：学术道德的底线
**(Slide 30)**

最后，Slide 30 列出了科研的“十诫”：
*   **Fabrication**：凭空捏造数据。
*   **Falsification**：篡改数据（比如孟德尔助手可能做的事）。
*   **Plagiarism**：抄袭。
*   **Shenanigans (诡计)**：
    *   **Double-dipping**：用同一组数据先筛选再分析。
    *   **Outlier excluding**：随意剔除不好看的数据（安斯库姆四重奏告诉我们要谨慎）。
    *   **P-hacking**：不断尝试各种统计方法直到 P<0.05（刚才的二项分布例子就是警示）。

---

### 全课总结（Lec 18 Review）

同学，这节课（Lec 18）其实是在教你**“统计学的防御术”**：

1.  **防御混杂因素**：看到不同组别比较（如中意死亡率），先想辛普森悖论（分层分析）。
2.  **防御完美数据**：看到 P 值太完美（接近1），先想是不是有人修饰了数据（孟德尔案例）。
3.  **防御盲目计算**：看到统计指标，先画图（安斯库姆四重奏）。
4.  **防御 P 值操纵**：明确实验设计（停止规则），否则同样的实验数据能算出截然相反的结论（二项 vs 负二项）。

复习完这些，你不仅掌握了知识，更具备了**审稿人**的眼光。祝你复习顺利！


# 19
## 1
你好！很高兴能以教授的身份带你回顾这节《生物医学统计学》（Biomedical Statistics）的核心内容。

从你提供的课件（Lecture 19）来看，这节课的前半部分（Slide 1-6）主要聚焦于统计推断中最迷人、也最违反直觉的基石——**贝叶斯法则（Bayes' Rule）**。

很多学生只把贝叶斯看作一个公式来背诵，但在生物医学领域，它是**诊断逻辑（Diagnostic Reasoning）**的数学本质。

我们将这部分内容拆解为三个核心模块来复习，我会特别指初那些**“看似简单，实则决定生死”**的关键点。

---

### 第一模块：贝叶斯法则的解构 (Slide 2)

**1. 课件回顾**
课件第2页展示了贝叶斯公式的推导，基于全概率公式（Total Probability）。
公式的核心形态是：
$$ P(A_i|B) = \frac{P(B|A_i)P(A_i)}{\sum P(B|A_k)P(A_k)} $$

**2. 教授划重点（学科核心逻辑）**
请你不要只盯着分母那长长的一串求和公式，我们要透过现象看本质。在医学统计中，这个公式在讲一件事：

*   **$P(A_i)$ (Prior, 先验概率)：** 在做检查之前，我们根据流行病学数据，认为病人患病的概率是多少？
*   **$P(B|A_i)$ (Likelihood, 似然度)：** 如果病人真有这个病，出现某种症状（或检测阳性）的概率是多少？
*   **$P(A_i|B)$ (Posterior, 后验概率)：** 这才是医生最关心的——**拿到检查结果后，病人真正患病的概率。**

**【极重要的隐性知识点】**
分母 $\sum P(B|A_k)P(A_k)$ 其实是一个**归一化常数（Normalizing Constant）**，也就是事件 $B$（如检测阳性）在整个人群中发生的总概率。这意味着，后验概率其实是**似然度与先验概率的乘积**在总概率中的占比。
> **口诀：** 后验概率 $\propto$ 似然 $\times$ 先验。这个概念是理解后续所有机器学习和高级统计推断的基础。

---

### 第二模块：诊断陷阱——灵敏度与患病率的博弈 (Slide 3)

**1. 课件回顾**
Slide 3 举了一个经典的 Covid 检测案例：
*   **Sensitivity (灵敏度/真阳性率)** $P(+|D) = 0.99$
*   **Specificity (特异度/真阴性率)** $P(-|\bar{D}) = 0.95$
*   **Prevalence (患病率/先验概率)** $P(D) = 0.001$ (0.1%)

计算结果显示，即便检测阳性，真正患病的概率 $P(D|+)$ 只有 **0.019 (1.9%)**。

**2. 教授划重点（真实世界的痛点）**
这页PPT是医学统计学中最著名的**“基本比率谬误”（Base Rate Fallacy）**。
很多医生和病人都容易混淆 $P(+|D)$ 和 $P(D|+)$。

*   **核心要点：** 当一个疾病在人群中非常罕见（Prevalence极低，如0.1%）时，即便检测手段非常精准（99%灵敏度），**阳性预测值（PPV）也会低得惊人**。
*   **学科意义：** 为什么我们不建议对普通人群进行罕见癌症的全面筛查？因为这会带来大量的**假阳性（False Positives）**，导致医疗资源的浪费和患者不必要的恐慌。
*   **记下来：** 离开实验室环境（Sensitivity），进入临床环境（PPV），**患病率（Prior）起决定性作用**。

---

### 第三模块：临床推理——先验概率的动态更新 (Slide 4-6)

**1. 课件回顾**
这是一个分层诊断的绝佳案例：
*   **场景：** 60岁老人，慢性咳嗽。鉴别诊断：正常、肺癌、结节病。
*   **Slide 4（普通人）：** 基于普通人群的先验概率，咳嗽后患癌概率仅为 **0.09**。
*   **Slide 5（重度吸烟者）：** 引入了**危险因素（Risk Factor）**。对于吸烟者，患癌的先验概率 $P(D_2)$ 从 0.001 飙升到了 0.015。
*   **Slide 6（重新计算）：** 在同样的症状（Likelihood不变）下，仅因为先验概率改变，吸烟者的患癌后验概率飙升到了 **0.711**。

**2. 教授划重点（贝叶斯思维的精髓）**
这个例子展示了贝叶斯统计不仅是计算，更是**思维方式**。

*   **重点一：先验不是固定的。**
    同样的症状（咳嗽），在不同背景的人身上（吸烟 vs 不吸烟），其代表的临床意义完全不同。作为统计学家或医生，你必须学会**根据患者特征（Covariates）调整先验概率**。这就是所谓的“分层”（Stratification）。
*   **重点二：证据的更新。**
    贝叶斯法则允许我们不断纳入新信息。
    $$ P(Disease) \xrightarrow{吸烟史} P(Disease|Smoker) \xrightarrow{咳嗽} P(Disease|Smoker, Cough) $$
    每一次信息的加入，都在更新我们的信念。这就是人工智能和循证医学的核心逻辑。

---

### 第四模块：直觉的修正——蒙提霍尔问题 (Slide 6 下半部分)

**1. 课件回顾**
经典的三门问题（Monty Hall Problem）。你选了门1，主持人开了门3（山羊），问你要不要换门2？
计算结果显示：不换的中奖率是 1/3，换的中奖率是 2/3。

**2. 教授划重点**
这里引入这个例子是为了强调：**人类的直觉在条件概率面前是不可靠的。**
*   关键在于主持人**“知道门后是什么”**这一条件。主持人打开门3并不是随机事件，而是提供了关于门2的信息（如果门1没车，车必然在门2）。
*   **学科意义：** 在生物统计中，我们经常面临看似“显然”但实则错误的结论。我们要学会用形式化的数学语言（定义 $H_i$, $C_i$, $X_i$）去推导，而不是依赖直觉。

---

### 总结 (Takeaway Message)

这节课前半段的灵魂在于**“信念的更新”**。

作为学生，你目前需要掌握的是：
1.  **熟练计算：** 能够手算贝叶斯公式，特别是在给定 Sensitivity/Specificity 时计算 PPV。
2.  **深刻理解：** 明白为什么**先验概率（Prevalence）**对最终结果有压倒性的影响（Slide 3 的教训）。
3.  **临床思维：** 理解概率是动态的，随着每一个新的临床特征（如吸烟史）的加入，我们需要重新评估风险（Slide 4-6 的演变）。

接下来的课程（Slide 7开始）进入了具体的**概率分布（Geometric, Hypergeometric）**，那是我们用来描述这些事件发生规律的工具，但**贝叶斯思想**是贯穿整个生物医学统计的灵魂。

## 2
你好！我们继续。如果说这节课前半段的贝叶斯法则是在教你**“如何像医生一样思考（推断）”**，那么后半段（Slide 7-8）则是在教你**“如何像生物信息学家一样建模”**。

这部分虽然篇幅短，只介绍了两个分布，但其中包含了**生物信息学（Bioinformatics）中最常用的统计检验原理**。很多学生会算公式却不懂其生物学意义，我来帮你捅破这层窗户纸。

我们将这部分划分为两个核心知识点：

---

### 第一模块：几何分布 (Geometric Distribution) —— 等待的艺术 (Slide 7)

**1. 课件回顾**
*   **定义：** $X \sim \text{Geometric}(\pi)$。描述的是在独立重复的伯努利试验中，**直到获得第1次成功（success）**所需要的试验次数（或者失败次数，课件公式看似是总次数）。
*   **公式推导：** 课件展示了从 PMF（概率质量函数）求和推导 CDF（累积分布函数）的过程：
    $$ F_X(x) = 1 - (1-\pi)^x $$
    这利用了等比数列求和公式。
*   **思考题：** "One-boy policy"（一孩政策/生男即止）下的 **Sex ratio（性别比）** 是多少？

**2. 教授划重点（学科核心陷阱）**

*   **直觉陷阱（Sex Ratio）：**
    这是统计学中最经典的面试题之一。课件问“Sex ratio?”，很多人的直觉是：既然生了男孩就停止，那男孩应该比较少？或者比较多？
    **答案是：1:1（在 $\pi=0.5$ 的前提下）。**
    *   **数学解释：** 几何分布的期望值 $E[X] = 1/\pi$。如果生男孩概率是0.5，平均每家生2个孩子（1男1女）。无论政策怎么规定“停止规则”，只要每次生育的性别概率独立且为0.5，大样本下的性别比永远由自然概率决定。
    *   **生物学意义：** 这告诉我们在设计生物实验时，**“停止规则”（Stopping Rule）**（例如：做实验直到出现阳性结果就停止）虽然影响样本量，但不一定改变单次事件的概率分布本质。

*   **分布特性：**
    你需要记住几何分布的一个特性：**无记忆性 (Memoryless Property)**。
    即便前10次都失败了，第11次成功的概率依然是 $\pi$，不会因为前面的失败而增加。这在模拟某些基因突变或随机到达过程时非常重要。

---

### 第二模块：超几何分布与 GO 富集分析 (Slide 7下半部分 - Slide 8)

**这是本节课后半段的绝对高潮。** 请务必打起十二分精神，因为这是**转录组测序（RNA-seq）**数据分析的灵魂。

**1. 场景引入 (Slide 7 下方)**
课件列出了 **GO (Gene Ontology)** 的三个分支：MF（分子功能）、BP（生物过程）、CC（细胞组分）。
这不是生物课的背诵内容，而是为下一页的统计模型做铺垫。我们在问：**“为什么我筛选出的差异基因里，这么多都和‘细胞核’有关？”** 这就是**富集分析（Enrichment Analysis）**。

**2. 课件回顾 (Slide 8)**
*   **模型：** 这是一个经典的“瓮中取球”模型（Urn Model），**不放回抽样**。
*   **参数映射（极重要）：**
    *   $N$ (All genes): 背景基因总数（例如人类2万个基因）。
    *   $M$ (Tag 'oncogene'): 背景中具有特定功能（如癌基因）的基因数。
    *   $K$ (Upregulated): 你实验中发现的差异表达基因总数（你手中的一把球）。
    *   $x$ 或 $k$: 你手中的基因里，恰好是癌基因的个数。
*   **公式：**
    $$ P(X \ge k) = \sum_{x=k}^{K} f_X(x) $$

**3. 教授划重点（真实存在的学科核心）**

*   **核心考点：为什么是 $\ge k$？（P-value 的本质）**
    注意看 Slide 8 最后的求和符号。我们在做富集分析时，计算的 P-value 不是“恰好抓到 $k$ 个癌基因”的概率，而是**“至少抓到 $k$ 个”**的概率。
    *   **逻辑：** 如果随机抓取，很难抓到这么多癌基因。现在我抓到了这么多（或更多），说明这不是随机的，而是与疾病相关的。这就是**Fisher精确检验（Fisher's Exact Test）**的数学基础。

*   **超几何 vs 二项分布：**
    学生常混淆这两个。
    *   **二项分布：** 抽样后**放回**（或者总体无穷大）。
    *   **超几何分布：** 抽样后**不放回**（基因总数是有限的）。
    *   **学科意义：** 在生物统计中，因为基因组大小（$N$）是有限的，当我们筛选了几百个基因（$K$）时，这种“有限总体”带来的概率变化不能忽略。因此，**做 GO/KEGG 富集分析必须用超几何分布，而不能用二项分布近似。**

*   **图表解读：**
    Slide 8 右侧的图展示了当 $N, M, K$ 变化时分布的形态。你会发现，随着 $N$ 变大，它看起来像正态分布（钟形曲线）。但在具体的基因分析中，我们要计算的是**尾部概率（Tail Probability）**，也就是图中右侧那个极小的阴影面积（P-value）。

---

### 总结 (Takeaway for the 2nd Half)

这半节课你只需要掌握两件事：

1.  **几何分布**处理的是“要试多少次才能成功”的问题，它是离散的，且具有无记忆性。
2.  **超几何分布**是生物信息学中**“富集分析”的数学基石**。
    *   以后你在论文里看到“Pathway Enrichment P-value < 0.05”，你要立刻反应过来：这实际上是在算一个超几何分布的尾部概率，证明这些基因出现在这里**不是巧合**。

复习建议：请手动把 Slide 8 的那个公式中的 $N, M, K, x$ 对应到真实的生物学概念上（背景基因、通路基因、差异基因、重叠基因），这是考试和科研中最容易搞混的地方。

# 20
## 1
你好！很高兴继续作为你的教授带你复习 Lecture 20 的前半部分（Slide 1-6）。

如果说上一节课我们在谈论“如何做推断”，那么这节课我们要深入**“数据的产生机制”**。在生物医学中，我们面对的大量数据都是**计数数据（Count Data）**——比如一段DNA上的突变数、一个细胞内的转录本数量、或者一年内发表的论文数。

这前半节课的核心主角只有一个：**泊松分布（Poisson Distribution）**，以及它在真实生物学世界中遇到的最大挑战——**过度离散（Overdispersion）**。

我们将其拆解为三个模块：

---

### 第一模块：泊松分布的诞生——稀有事件的法则 (Slide 2-4)

**1. 课件回顾**
*   **场景：** 课件用**基因突变**（Mutation rate）作为引入。在一个长为 $N$ 的基因组序列中，每个位点突变的概率 $\pi$ 很低。
*   **数学推导（Slide 4）：**
    这几页PPT展示了一个经典的极限过程：当二项分布（Binomial）的试验次数 $N \to \infty$（基因组很长），而成功概率 $\pi \to 0$（突变率极低），且它们的乘积 $\lambda = N\pi$ 保持为一个常数时，二项分布就演变成了泊松分布。
*   **公式：**
    $$ P(X=x) = \frac{\lambda^x e^{-\lambda}}{x!} $$
*   **核心性质：** $E(X) = Var(X) = \lambda$。**（期望等于方差）**

**2. 教授划重点（学科核心逻辑）**
*   **为什么叫“稀有事件法则”？**
    不要死记公式。你要理解它的物理意义：在一段固定的时间或空间内，如果事件发生是**相互独立**的，且发生概率极低，那么这件事发生的次数就服从泊松分布。
*   **生物学直觉：**
    当你拿到测序数据，问“这段 1kb 的序列上有多少个突变？”或者“显微镜视野里有多少个细胞？”时，你的第一反应应该是泊松分布。
*   **那个极限推导意味着什么？**
    它意味着我们不需要知道具体的 $N$（总碱基数）和 $\pi$（具体突变率），只要知道**平均发生次数 $\lambda$**，就可以算出各种情况的概率。这对生物统计极其实用，因为 $N$ 往往大到难以精确计数。

---

### 第二模块：理想 vs. 现实——过度离散 (Overdispersion) (Slide 5)

**这是前半节课最重要、也是最容易被学生忽视的概念。**

**1. 课件回顾**
*   **案例：** 统计2024年中国学者发表在 *Cell* 上的论文总数 $X$。
*   **假设变化：** 之前我们假设 $\lambda$ 是常数。但在这里，Slide 5 指出 **$\lambda$ is a random variable**（$\lambda$ 本身也是个随机变量）。
*   **公式推导（方差分解）：**
    $$ Var(X) = E[Var(X|\lambda)] + Var(E[X|\lambda]) = E[\lambda] + Var(\lambda) > E[\lambda] $$
    因为 $Var(\lambda) > 0$，所以最终的 $Var(X)$ 必定大于均值 $E[X]$。

**2. 教授划重点（真实世界的痛点）**
*   **什么是 Overdispersion？**
    教科书上的泊松分布要求 **均值 = 方差**。但在真实的生物数据中，我们几乎总是发现 **方差 >> 均值**。这叫过度离散。
*   **为什么会发生？（Slide 5 的深层含义）**
    以发 *Cell* 为例，每篇文章被接收的概率不是恒定的。它取决于课题热度、审稿人心情、合作者名气等。这些**未被观测到的异质性（Unobserved Heterogeneity）**导致了 $\lambda$ 本身就在波动。
*   **学科意义（极重要）：**
    如果你在做差异表达分析（如 RNA-seq）时，强行使用标准的泊松分布模型，你就会**低估数据的波动性**，从而导致计算出的 P-value 偏小，产生大量的**假阳性（False Positives）**。
    > **一句话考点：** 当 $\lambda$ 不固定时，混合后的分布方差会大于均值，这种现象叫过度离散。

---

### 第三模块：转录爆发——泊松噪声 vs. 生物学信号 (Slide 6)

**1. 课件回顾**
*   **左图：** $\mu = 57.3, \sigma^2 = 62.5$。均值约等于方差。
    这是一个完美的泊松过程，代表 RNA 聚合酶装载是**随机、独立**的。
*   **右图：** $\mu = 60.1, \sigma^2 = 490.2$。**方差远大于均值**。
    这显示了极强的 Overdispersion。
*   **图注：** "Poisson noise" 也就是那条虚线，代表理想状态。

**2. 教授划重点**
*   **转录爆发 (Bursting)：**
    Slide 6 右图实际上展示了基因转录的**爆发性（Burstiness）**。基因不是匀速像流水线一样生产 RNA（那样就是左图），而是像“阵发”一样，一会儿狂转录，一会儿休息。
*   **统计作为显微镜：**
    通过比较方差和均值的关系（Fano Factor = $\sigma^2 / \mu$），我们甚至不需要显微镜，就能推断出分子的微观动力学机制。
    *   $\sigma^2 \approx \mu$ $\rightarrow$ 随机独立过程（Constitutive expression）。
    *   $\sigma^2 \gg \mu$ $\rightarrow$ 爆发式过程（Bursting / Regulated expression）。

---

### 总结 (Takeaway Message)

复习这前半段，你需要建立以下认知：

1.  **基准线：** **泊松分布**是计数数据的基准模型，特征是 **均值 = 方差**。
2.  **红灯警示：** 在处理真实生物数据（如 RNA-seq read counts）时，**Overdispersion（方差 > 均值）是常态**。
3.  **原因：** 过度离散通常是因为背后的概率参数 $\lambda$ 并不是固定的，而是受环境、个体差异影响在波动。
4.  **后果：** 忽略过度离散会导致统计检验过于自信（P值太小）。这也是为什么后续课程（或高级分析中）会引入 **负二项分布（Negative Binomial）** 来解决这个问题的原因。
## 2
你好！我们继续。

如果说前半节课的泊松分布解决了**“数数（Counting）”**的问题（比如：这个细胞里有几个突变？），那么后半节课（Slide 7-11）则进入了**“连续（Continuous）”**的世界。

我们要探讨两个问题：
1.  **时间问题：** 下一次突变发生还需要**等多久**？ $\rightarrow$ **Gamma 分布**
2.  **概率问题：** 一个事件发生的**概率本身**是如何分布的？ $\rightarrow$ **Beta 分布**

这部分数学推导较多，但我会带你跳过繁琐的微积分，直接抓取其生物统计的核心意义。

---

### 第一模块：Gamma 分布——等待的艺术 (Slide 7-9)

**1. 课件回顾**
*   **Slide 7 (上)：** 从泊松分布导出。如果你在等第1个事件发生（$1^{st}$ event），等待时间 $t$ 服从什么分布？结果是指数分布（Exponential）。
*   **Slide 7 (下)：** 推广一下。如果你不是等第1个，而是等**第 $\alpha$ 个事件**发生（$\alpha^{th}$ event），等待时间 $t$ 服从什么分布？这就是 **Gamma 分布**。
*   **Slide 8-9：** 展示了 Gamma 分布的概率密度函数（PDF）和图像。
    *   $\alpha$ (Shape parameter)：形状参数，对应“你要等的事件个数”。
    *   $\beta$ (Scale parameter)：尺度参数，对应“事件发生的平均间隔”。

**2. 教授划重点（学科核心逻辑）**
*   **泊松与Gamma的“硬币两面”：**
    这是统计学中最美妙的对称之一，一定要理解：
    *   **泊松分布：** 固定时间，问发生了多少次？（离散的）
    *   **Gamma分布：** 固定发生次数，问花了多少时间？（连续的）
    *   **生物学例子：**
        *   泊松：如果你问“1分钟内神经元放电多少次？”
        *   Gamma：如果你问“神经元累积放电10次，需要多长时间？”（Inter-spike interval analysis 经常用到这个）。

*   **为什么要引入 Gamma 函数 $\Gamma(\alpha)$？**
    Slide 8 特别推导了 $\Gamma(\alpha) = (\alpha-1)!$。这是为了把“阶乘”这个概念从整数推广到实数。这意味着 Gamma 分布不仅可以模拟“等第3个事件”，还可以模拟“等第3.5个单位的量”。

*   **图像直觉 (Slide 9)：**
    看 Slide 9 的图，当 $\alpha$ 很小时（如 0.5），概率密度集中在 0 附近（极度偏斜）；当 $\alpha$ 变大时（如 4），图像开始变得像正态分布（钟形）。这符合中心极限定理——等的事情越多，时间的总和越趋于稳定。

---

### 第二模块：Beta 分布——概率的概率 (Slide 10-11)

**这是贝叶斯统计中的“明星分布”，请务必重点标记。**

**1. 课件回顾**
*   **Slide 10 的标题：** "Probability of probability"（概率的概率）。这句话是精髓。
*   **定义：** Beta 分布定义在区间 $[0, 1]$ 上。这正是“概率”的取值范围。
*   **物理起源（Order Statistic）：**
    课件用了一个很巧妙的角度引入：如果你从均匀分布 $U(0,1)$ 中随机抽取 $n$ 个数，并把它们从小到大排好序。那么，**第 $k$ 小的那个数**（$X_{(k)}$）出现在什么位置？
    答案是：它服从 $Beta(k, n-k+1)$ 分布。

**2. 教授划重点（真实应用场景）**
*   **为什么要研究“概率的分布”？**
    回到 Lecture 19 的贝叶斯。如果我问你：“这个硬币正面的概率 $\pi$ 是多少？”
    *   频率学派会说：是一个定值，比如 0.5。
    *   **贝叶斯学派（本课程的重点）会说：** 我不确定 $\pi$ 是多少，但我认为它大概率在 0.5 附近，小概率在 0.1 或 0.9。**这种对 $\pi$ 的“信念”本身，就用 Beta 分布来描述。**

*   **Slide 11 的生物学意义：**
    假设你在研究某种药物的**有效率（Efficacy）**。
    *   你做了 $n$ 个病人的实验，治好了 $k$ 个。
    *   那么这种药真实的有效率 $\pi$ 是多少？
    *   我们不会只给出一个数字（如 $k/n$），我们会给出一个**Beta 分布**。
    *   Slide 11 的图展示了这一点：随着样本量增加（$n$ 变大），曲线越来越窄（Variance 变小），说明我们对有效率的估计越来越**确信**。

---

### 总结与升华 (The Big Picture)

这节课（Lecture 20）实际上是在构建**广义线性模型（GLM）**和**贝叶斯推断**的地基。

作为教授，我希望你把这四个分布串起来记忆：

1.  **泊松 (Poisson)：** 这种病**发生几次**？（计数，Count）
2.  **指数 (Exponential)：** 下个病人**还要等多久**？（无记忆等待，Time）
3.  **Gamma：** 凑齐10个病人**还要等多久**？（累积等待，Time）
4.  **Beta：** 这种病的**治愈率**大概是多少？（比例/概率，Proportion）

**最后的点睛之笔：**
在贝叶斯统计中：
*   **Gamma 分布**常作为**泊松分布**参数 $\lambda$ 的先验分布（Conjugate Prior）。
*   **Beta 分布**常作为**二项分布**参数 $\pi$ 的先验分布。

这就是为什么这节课要把它们放在一起讲。理解了这一点，你才算真正读懂了这节课的逻辑闭环。

# 21
## 1
你好！我是你的教授。很高兴带你复习 Lecture 21 的前半部分（Slide 1-32）。

这节课是统计学的**分水岭**。在此之前，我们是在学“概率”（已知参数 $\theta$，预测数据 $X$）；从这节课开始，我们正式进入**“统计推断”（Statistical Inference）**（已知数据 $X$，反推参数 $\theta$）。

前半段的内容非常扎实，涵盖了**频率学派（Frequentist）**的核心估计算法。我们将这32页PPT的逻辑梳理为四个核心模块。

---

### 第一模块：评估者的修养——什么样的估计是好估计？(Slide 3-10)

**1. 核心概念辨析**
*   **Estimator (估计量, $\hat{\theta}$)** vs **Parameter (参数, $\theta$)**:
    *   $\theta$ 是上帝才知道的真理（如人群真实的患癌率），是一个**常数**。
    *   $\hat{\theta}$ 是我们要构建的公式（统计量），是一个**随机变量**。
*   **MSE (均方误差) 的分解 (Slide 6)**：
    这是本章的理论基石，必须背下来：
    $$ MSE = E[(\hat{\theta} - \theta)^2] = Var(\hat{\theta}) + [Bias(\hat{\theta})]^2 $$
    **教授解读：** 一个好的估计，既要“准”（无偏，Bias小），又要“稳”（方差小，Variance小）。

**2. 靶心图解 (Slide 7)**
这张图是统计学经典。
*   **Unbiased (无偏)**：平均落点在靶心。
*   **Efficient (有效/高精度)**：弹孔非常集中。
*   **重点结论 (Slide 8-10)**：
    *   样本均值 $\bar{X}$ 和样本方差 $S^2$ 都是**无偏估计**。
    *   但是！在 Slide 9-10 的泊松分布案例中，我们发现用样本均值 $\bar{X}$ 去估计 $\lambda$ 的方差比用样本方差 $S^2$ 更小（$Var(\bar{X}) < Var(S^2)$）。
    *   **学科核心意义：** 这就是为什么在基因测序数据（泊松分布）中，即便 $E=Var$，我们也更倾向于用 Count 的**均值**来作为强度的估计，因为它的**效率（Efficiency）更高**，犯错波动更小。

---

### 第二模块：矩估计 (MME)——简单粗暴的模仿 (Slide 11-13)

**1. 方法论**
*   **思想：** “样本长什么样，总体就该长什么样”。
*   **做法：** 令样本的一阶矩（均值）、二阶矩（方差+均值平方）等于总体的期望和方差，解方程组。

**2. 教授评价**
MME 的优点是计算简单，不需要知道复杂的似然函数。但它的缺点是**效率通常较低**，丢失了数据中的很多信息。在生物医学统计中，通常作为复杂迭代算法（如EM算法）的**初始值 (Initial Value)**。

---

### 第三模块：极大似然估计 (MLE)——寻找最可能的真理 (Slide 14-20)

这是本节课的**绝对主角**。

**1. 核心直觉**
MLE 问的问题是：**“哪一组参数 $\theta$，能让我手中这组数据出现的概率最大？”**
$$ \hat{\theta}_{MLE} = \arg\max L(\theta|x) $$

**2. 关键技巧：对数似然 (Log-Likelihood)**
*   因为概率是连乘 $\prod P(x_i)$，求导极其痛苦。
*   取对数后变成连加 $\sum \log P(x_i)$，求导瞬间简单。
*   Slide 16 的 Bernoulli 实验（5只老鼠死了1只）是教科书级的入门计算，必须掌握。

**3. 生物学真实案例：基因调控网络 (Slide 18-20)**
*   **现象：** 只有少数“明星基因”（Hub genes）连接了大量下游基因，绝大多数基因只有寥寥几个连接。
*   **分布：** 这不符合正态分布，而是**幂律分布 (Power Law / Pareto Distribution)**，$P(x) \sim x^{-\gamma}$。
*   **MLE的应用：** 针对这种长尾分布，用简单的均值估计（MME）效果极差，必须构建 Pareto 分布的似然函数进行 MLE 求解（Slide 20 的推导），才能准确找到那个 $\alpha$（幂指数）。

---

### 第四模块：真实世界的麻烦——删失与截断 (Slide 21-32)

**这是最体现“生物医学特色”的部分，也是考试和科研的难点。**

**1. 概念区分 (Slide 21)**
很多同学分不清 Censored 和 Truncated：
*   **Censored (删失)：** 数据点**存在**，但我不知道确切数值，只知道范围。
    *   *例子：* 生存分析。实验结束时老鼠还没死。我知道它的寿命 $T > 100$ 天，但不知道具体是多少。这叫右删失（Right-censored）。
*   **Truncated (截断)：** 数据点如果不满足条件，根本就**观测不到**（直接从样本里消失了）。
    *   *例子：* 显微镜观察。太小的细胞看不见。或者 RNA-seq 中，表达量为 0 的基因可能根本不在数据表里。

**2. MLE 如何处理删失？(Slide 22-26)**
*   **核心逻辑：** 没死的老鼠也要贡献信息！
    *   死了的老鼠贡献概率密度 $f(x)$。
    *   没死的老鼠贡献累积概率 $1-F(T)$（即存活概率）。
*   **Slide 26 (Hazard Rate)：** 引入了风险率函数 $h(t)$，这是生存分析的灵魂，用于描述“瞬间死亡率”。

**3. MLE 如何处理截断？(Slide 28-29)**
*   **零截断泊松 (Zero-Truncated Poisson, ZTP)：**
    *   *场景：* 假设我们在研究某种罕见病蛋白。只有当蛋白数 $\ge 1$ 时我们才能检测到。如果蛋白数为 0，仪器读不出数。
    *   *修正：* 此时必须把泊松分布的 $P(X=0)$ 这一项切掉，剩下的概率重新归一化（分母除以 $1-e^{-\lambda}$）。
    *   **后果：** 如果你忽略截断，直接用普通泊松MLE，你会**高估** $\lambda$（因为你丢掉了所有的 0）。

**4. 梯度下降 (Gradient Descent, Slide 30-32)**
*   当 MLE 的方程太复杂（比如 ZTP 分布，包含 $e^\lambda$ 在分母），无法手算出解析解（Closed-form solution）时，我们就需要计算机通过梯度下降法去逼近最优解。这是现代计算统计学的常规操作。

---

### 总结 (Takeaway Message)

复习前半段，请务必建立以下认知：

1.  **好估计的标准：** 瞄准靶心（无偏），且手不抖（方差小）。
2.  **MLE 是王者：** 它是频率学派最常用的工具，虽然计算可能复杂，但理论性质最好。
3.  **生物数据的复杂性：** 真实数据往往是不完美的。
    *   看到“实验没做完但数据要用”，想到 **Censorship**。
    *   看到“检测限以下数据丢失”，想到 **Truncation**。
    *   在这些情况下，必须修改似然函数 $L(\theta)$，否则结果就是错的。

掌握了 MLE，你就有资格进入后半节课，去挑战贝叶斯学派的 MAP 了。准备好了吗？


# 22
## 1
你好！很高兴继续带你深入 Lecture 22。

如果说上一节课（Lecture 21）是贝叶斯统计的“入门”，学会了 MLE 和 MAP；那么这一节课（Lecture 22）就是贝叶斯统计的**“进阶”**，我们进入了**分层模型（Hierarchical Models）**和**复合模型（Compound Models）**的领域。

这前半节课（Slide 1-18）解决了一个生物统计中最令人头疼的问题：**当你只有很少的样本（Small Sample Size）时，如何做出靠谱的推断？**

我们将这部分内容拆解为三个核心模块来复习：

---

### 第一模块：高斯共轭与“收缩”的艺术 (Slide 2-6)

**1. 场景回顾**
*   **例子 (Slide 2)：** 放疗后病人的体重。
*   **已知：** 先验分布 $N(49.5, 0.9^2)$（基于过往经验），似然 $N(\mu_w, 1^2)$（基于测量数据）。
*   **计算：** 输入一个测量值 $x=47$，求后验均值。

**2. 核心公式 (Slide 5)**
这是本节课最需要理解（而不只是死记）的公式。后验均值 $\mu_{post}$ 是先验均值 $\mu$ 和样本均值 $\bar{x}$ 的**加权平均**：
$$ \mu_{post} = \frac{\frac{1}{\tau^2}\mu + \frac{n}{\sigma^2}\bar{x}}{\frac{1}{\tau^2} + \frac{n}{\sigma^2}} $$
*   **权重是什么？** 是**精度 (Precision)**，即方差的倒数。
    *   如果你先验很准（$\tau^2$ 小），先验权重就大。
    *   如果你样本量大（$n$ 大），样本权重就大。

**3. 教授划重点（学科核心概念：Shrinkage）**
*   **收缩 (Shrinkage)：**
    观察 Slide 6 的结果。样本观测值是 47.5，但后验均值是 48.38。
    这意味着，估计值被先验从 47.5 **“拉”**（或者叫收缩）回了 49.5 的方向。
*   **意义：** 贝叶斯估计不仅仅是计算，更是一种**折中**。它防止你因为一次偶然的极端测量（比如 47.5）就对真实情况做出过激的判断。

---

### 第二模块：复合分布的定义 (Slide 7-8)

**1. 概念 (Slide 7)**
*   **公式：** $f_X(x) = \sum f(X|\theta)g(\theta)$
*   **直觉：** 这不是简单的 $X$ 分布，而是 $X$ 的参数 $\theta$ 自己也在变。
*   **例子 (Slide 8)：** 学生的体重。
    *   如果我们不分男女，直接看总体，体重的分布是两个正态分布的混合（双峰）。
    *   这就是一个简单的复合模型：性别是隐变量 $\theta$，体重是观测值 $X$。

---

### 第三模块：Beta-Binomial 模型——解决小样本的方差问题 (Slide 9-18)

**这是前半节课的绝对高潮，也是生物信息学（如 scRNA-seq 分析）的核心算法基础。**

**1. 现实痛点 (Slide 9)**
*   **数据：** 40家医院的 Covid-19 死亡率。
*   **问题：** 有一家新医院，收治10人，死3人 ($3/10$)。
    *   直接算 MLE：死亡率 = 30%。
    *   但这合理吗？其他医院都在 5%~10% 左右。30% 很可能是因为样本太小（$n=10$）造成的偶然波动。
*   **目标：** 我们需要借用其他 40 家医院的信息，来修正这第 41 家医院的估计。

**2. 模型构建 (Slide 10)**
*   **分层结构：**
    1.  每个医院有自己的死亡率 $\pi_i$。
    2.  但这 40 个 $\pi_i$ 不是乱来的，它们都服从同一个分布：**Beta($\alpha, \beta$)**。
    3.  观测数据 $d_i$ 服从二项分布 Bin($n_i, \pi_i$)。

**3. 经验贝叶斯 (Empirical Bayes) 求解 (Slide 11-12)**
*   **关键思想：** 我们不知道 Beta 分布的长相 ($\alpha, \beta$)，但我们可以利用这 40 家医院的数据把它们**反推**出来！
*   **方法一：矩估计 (MME)**
    *   算出所有医院死亡率的均值和方差。
    *   令样本均值/方差 = Beta分布的理论均值/方差。
    *   解出 $\hat{\alpha}=0.923, \hat{\beta}=11.03$。
    *   **教授点评：** 这意味着我们找到了所有医院共享的“平均规律”。

**4. 结果修正 (Slide 13-14)**
*   **修正后的第41家医院：**
    *   先验：Beta(0.923, 11.03) （来自大家的规律）
    *   数据：10死3。
    *   后验：Beta(0.923+3, 11.03+7) $\approx$ Beta(3.9, 18.0)
    *   **MAP/均值：** 修正后的死亡率约为 **0.179 (17.9%)**。
*   **神奇的图 (Slide 14 - 必考点)：**
    *   横轴是 MLE（直接算），纵轴是 Posterior Mean（修正后）。
    *   红线是 $y=x$。
    *   **蓝线 (Posterior Estimate)：** 是一条斜率小于1的线。
    *   **核心现象：**
        *   样本量大的点（实心圈），在红线附近（数据说了算）。
        *   样本量小的点（空心圈），被大力**拉向中心均值**（Shrink relative to MLE）。
    *   **结论：** Beta-Binomial 模型自动实现了“信数据还是信经验”的智能切换。

**5. 进阶求解：ML-II Prior (Slide 15-18)**
*   除了矩估计 (MME)，我们还可以用更严谨的 **ML-II (Type II Maximum Likelihood)**。
*   **做法：** 积分积掉 $\pi$，直接最大化观测数据 $Data$ 关于超参数 $\alpha, \beta$ 的似然函数 (Marginal Likelihood)。
*   Slide 18 的等高线图展示了计算机是如何找到最优的 $\alpha$ 和 $\beta$ 的。

---

### 总结 (Takeaway Message)

复习这前半段，你需要明白一个核心逻辑：**Empirical Bayes (经验贝叶斯)**。

1.  **以前 (Lecture 21)：** 先验 $\alpha, \beta$ 是拍脑袋定的（比如 Beta(1,1)）。
2.  **现在 (Lecture 22)：** 先验 $\alpha, \beta$ 是**从整体数据中学习出来的**。
3.  **应用场景：** 当你有一批同类数据（如40家医院，或者RNA-seq中的20000个基因），其中某些样本量很小（Low counts）时，利用**Beta-Binomial**模型，可以让大样本帮助小样本，把那些因为偶然因素偏得离谱的估计值，**收缩 (Shrink)** 回合理的范围内。

这就是现代高通量生物数据分析中“稳健性”的来源。准备好进入后半节课的 Poisson-Gamma 模型了吗？那将是处理计数数据（Sequencing counts）的利器。

## 2
你好！我们继续。

如果说前半节课的 **Beta-Binomial** 模型是解决“比例/概率”（Proportion）问题的神器（如死亡率、甲基化率），那么后半节课（Slide 19-31）的 **Poisson-Gamma** 模型则是解决“计数”（Count）问题的王者（如物种数量、测序Read数）。

这部分内容直接对应了生物生态学中的经典问题，以及现代基因组学中的**差异表达分析**。我们将这13页PPT的逻辑梳理为三个核心模块。

---

### 第一模块：Poisson-Gamma 模型——计数的混合 (Slide 19-22)

**1. 场景引入：钓鱼/捉虫 (Slide 20)**
*   **数据：** Fisher (1943) 的经典蝴蝶捕捉数据。
    *   $x=1$ (抓到1次的物种): 118种
    *   $x=2$ (抓到2次的物种): 74种
    *   ...
*   **问题：** 明年我们会抓到多少**新物种**？（这本质上是在做**未知预测** Unseen Prediction）。

**2. 为什么单一泊松不够？(Slide 21)**
*   **计算：** 如果假设所有物种被抓的概率一样（单一 $\lambda$），算出来的均值 $\bar{X}=6.6$，方差 $S^2=38.8$。
*   **矛盾：** 泊松分布要求 **均值 $\approx$ 方差**。现在 **方差 $\gg$ 均值 (Overdispersion)**。
*   **原因：** 物种是异质的！有的蝴蝶傻（容易被抓），有的蝴蝶精（很难被抓）。每个物种 $i$ 都有自己的 $\lambda_i$。

**3. 复合模型构建 (Slide 22)**
*   **分层结构：**
    1.  每个物种的捕捉率 $\lambda_i$ 不同。
    2.  这些 $\lambda_i$ 服从一个 **Gamma($\alpha, \beta$)** 分布（先验）。
    3.  给定 $\lambda_i$，抓到的次数 $X_i$ 服从 **Poisson($\lambda_i$)**。
*   **名称：** 这种组合在统计学上叫 **负二项分布 (Negative Binomial Distribution)**。记住这个名字，它是 RNA-seq 分析软件（DESeq2, edgeR）的数学心脏。

---

### 第二模块：ML-II 估计——寻找超参数 (Slide 23-24)

**1. 边缘分布推导 (Slide 23)**
*   这页PPT展示了极其华丽的积分技巧（把 $\lambda$ 积掉）。
*   **结果：** 得到的边缘分布 $P(X=x)$ 不再含有 $\lambda$，只剩下超参数 $\alpha, \beta$ 和 $x$。
    $$ P(X=x) = \frac{\Gamma(\alpha+x)}{x!\Gamma(\alpha)} \left(\frac{\beta}{\beta+1}\right)^\alpha \left(\frac{1}{\beta+1}\right)^x $$
    这就是负二项分布的公式形式之一。

**2. 求解超参数 (Slide 24)**
*   有了 $P(X=x)$ 的公式，我们就可以用 **ML-II (Type II Maximum Likelihood)** 方法。
*   **目标：** 找到一组 $\hat{\alpha}, \hat{\beta}$，使得观测到的数据（118个x=1, 74个x=2...）出现的概率最大。
*   **结果：** $\hat{\alpha}=0.10, \hat{\beta}=-89.8$。（注意：这里 $\beta$ 的定义可能涉及 parameterization 的不同，通常 $\alpha, \beta$ 应为正，但生物统计中有时会有不同参数定义，此处关注PPT中的拟合曲线即可）。
*   **看图 (Slide 24)：** 紫线（拟合线）完美穿过了所有数据点。这说明 Poisson-Gamma 模型成功捕捉了数据的 Overdispersion 特征。

---

### 第三模块：神奇的预测——Good-Turing 估计的贝叶斯版本 (Slide 25-27)

这是整个课程中最具“魔法”色彩的部分：**预测未见之物 (Predicting the Unseen)**。

**1. 问题重述 (Slide 25)**
*   **Predict:** $P(X_{this}=0 \text{ and } X_{next} > 0)$。
    即：今年没抓到（$x=0$，所以是新物种），但明年抓到了（$x>0$）的概率。

**2. 推导逻辑 (Slide 26-27)**
*   这个推导非常硬核，利用了后验分布的期望。
*   **关键公式 (Slide 27 上方)：**
    $$ E[\text{new species}] / S_{total} = \left(\frac{1}{1+\hat{\beta}}\right)^{\hat{\alpha}} - \left(\frac{1}{2+\hat{\beta}}\right)^{\hat{\alpha}} $$
*   **更惊人的巧合 (Slide 27 下方)：**
    经过复杂的贝叶斯推导，我们发现：
    $$ E[\text{new species}] \approx P(X=1) \times S_{total} = \text{Count of species caught exactly once} $$
    即：**明年发现的新物种数量，大约等于今年只抓到过1次的物种数量 (Singletons)。**
    *   **数据验证：** 也就是 Slide 27 中算出的 **118**。

**3. 教授划重点**
这是一个著名的统计学结论（与阿兰·图灵有关）。它告诉我们，那些**极其罕见的事件（Singletons）**包含了关于**未知世界（Unseen）**的最重要信息。
在单细胞测序中，我们用这个原理来估计：如果你再测序深一点，还能发现多少种新的细胞类型？

---

### 第四模块：无信息先验 (Uninformative Priors) (Slide 28-31)

最后几页PPT讨论了一个哲学问题：如果我们真的**一无所知**，先验该怎么设？

**1. 位置参数 (Slide 29-30)**
*   对于均值 $\mu$（位置参数），“一无所知”意味着它是 $-\infty$ 到 $+\infty$ 上的常数。
*   **Improper Prior:** 这种分布积分为无穷大，不是合法的概率密度，但在计算后验时通常没问题（因为似然函数收敛很快）。

**2. 尺度参数 (Slide 31)**
*   对于方差 $\sigma^2$（尺度参数），“一无所知”通常设为 $P(\sigma^2) \propto 1/\sigma^2$。
*   **Jeffreys Prior:** 这是一种对于参数变换保持不变的先验设定，是贝叶斯统计中追求客观性的尝试。

---

### 总结 (Takeaway Message)

复习完整个 Lecture 22，你的知识库里应该增加了两件重型武器：

1.  **Beta-Binomial 模型：** 用于处理**比例数据**（Proportion）。
    *   *核心功能：* **Shrinkage (收缩)**。利用群体信息修正小样本的极端值。
2.  **Poisson-Gamma (负二项) 模型：** 用于处理**计数数据**（Count）。
    *   *核心功能：* **Overdispersion (过度离散)**。解决方差远大于均值的生物学异质性问题。
    *   *应用：* 预测未知物种、RNA-seq 差异表达分析。

这节课的内容是生物统计从“初级计算”迈向“高级建模”的阶梯。请务必理解 **Empirical Bayes** 的思想：**先验不是瞎猜的，是从数据里学出来的。**

# 23
## 1
你好！我是你的教授。很高兴带你进入 Lecture 23 的学习。

这节课的前半部分（Slide 1-11）实际上是对上一节课（Lecture 22）**Poisson-Gamma 复合模型**的收尾和升华。我们不仅仅是在做数学推导，而是在解决生态学和基因组学中一个极具浪漫色彩的问题——**如何预测未见之物（Predicting the Unseen）**。

随后的 Slide 12 开启了一个全新的篇章：**多维分布（Multivariate Distributions）**。

我们将这前半段拆解为三个核心模块：

---

### 第一模块：回顾与痛点——为什么泊松分布失效了？(Slide 2-4)

**1. 场景回顾**
*   **数据 (Slide 2)：** Fisher (1943) 的蝴蝶捕捉数据。
    *   抓到1次的物种：118种 (Singletons)
    *   抓到2次的物种：74种 (Doubletons)
    *   ...
*   **计算 (Slide 4)：**
    *   样本均值 $\bar{X} = 6.6$
    *   样本方差 $S^2 = 38.8$
*   **核心矛盾：** **方差 $\gg$ 均值**。
    *   如果你强行用泊松分布（Poisson）建模，方差应该等于均值（约6.6）。但现实数据的波动（38.8）远远超出了泊松分布的解释能力。这就是 **Overdispersion（过度离散）**。

**2. 教授划重点（学科核心逻辑）**
*   **生物学解释：** 为什么会 Overdispersion？因为**异质性（Heterogeneity）**。
    *   不同的物种，其“被捕捉的概率”（$\lambda$）是天差地别的。有的物种满天飞（$\lambda$大），有的物种很隐蔽（$\lambda$小）。
    *   当我们把所有物种混在一起统计时，单一的 $\lambda$ 无法描述这种差异，必须引入**复合模型**。

---

### 第二模块：Poisson-Gamma 模型与负二项分布 (Slide 5-8)

**1. 模型构建 (Slide 5)**
*   **层级结构：**
    1.  **个体差异：** 每个物种 $i$ 有自己的速率 $\lambda_i$。
    2.  **先验分布：** 这些 $\lambda_i$ 服从 **Gamma($\alpha, \beta$)** 分布。
    3.  **观测数据：** 给定 $\lambda_i$，抓到的次数 $X_i$ 服从 **Poisson($\lambda_i$)**。

**2. 数学推导 (Slide 6)**
*   这页PPT展示了如何把未知的 $\lambda$ 积掉（Marginalization）。
*   **结论（必考点）：** Poisson-Gamma 的边缘分布（Marginal Distribution）就是 **负二项分布（Negative Binomial Distribution）**。
    *   公式里只剩下 $\alpha, \beta$ 和 $x$，不再有 $\lambda$。
    *   这就是为什么你在做 RNA-seq 差异表达分析（如 DESeq2, edgeR）时，软件说明书会告诉你数据服从负二项分布，而不是泊松分布。

**3. ML-II 参数估计 (Slide 7-8)**
*   **目标：** 我们不知道 $\alpha$ 和 $\beta$ 是多少。
*   **方法：** 利用**观测到的频数分布**（118个1，74个2...）来反推。
*   通过最大化 Slide 7 那个复杂的对数似然函数，计算机算出了 $\hat{\alpha}=0.10, \hat{\beta}=-89.8$（注意：这里的 $\beta$ 数值取决于具体的参数定义方式，重点是看 Slide 8 的图）。
*   **效果：** Slide 8 的曲线（拟合线）完美穿过了数据点，证明负二项分布能够很好地拟合这种高方差数据。

---

### 第三模块：预测未见之物——生态学的魔法 (Slide 9-11)

这是本节课最精彩的部分。我们建立了模型，不仅是为了拟合已知的数据（抓到的蝴蝶），更是为了**推断未知的数据（没抓到的蝴蝶）**。

**1. 问题定义 (Slide 9)**
*   **问题：** 明年我们会抓到多少**新物种**？
*   **数学表达：** $P(X_{next} > 0 | X_{this} = 0)$。即今年次数为0，明年次数大于0。

**2. 推导逻辑 (Slide 10-11)**
*   利用贝叶斯后验，我们可以计算出那些“今年没被抓到（$x=0$）”的物种，其潜在的 $\lambda$ 分布。
*   Slide 11 展示了最终的计算结果：
    *   基于拟合出的 $\alpha, \beta$ 参数，模型预测明年抓到的新物种数量约为 **46.95** 种。
*   **Good-Turing 估计的影子：**
    虽然 Slide 上是用 Gamma 参数算的，但这里隐含了一个著名的统计学直觉（Good-Turing frequency estimation）：**未见物种的概率质量，大约等于只出现一次的物种（Singletons）的总比例。**
    *   你看 Slide 11 中提到 $E[X=1] = 118$。这118个只出现一次的物种，是预测未见物种的关键线索。

**3. 教授划重点**
*   **应用场景：** 这不仅仅是捉蝴蝶。
    *   **单细胞测序：** 我测了 5000 个细胞，发现了几种细胞类型。如果我再测 5000 个，还能发现多少**新类型**？
    *   **TCR/BCR 库分析：** 免疫组库里还有多少未知的克隆？
    *   这都是 Poisson-Gamma 模型大显身手的地方。

---

### 第四模块：多维分布的开端 (Slide 12)

**1. 维度升级**
*   Slide 12 标志着课程从**一维（Univariate）**进入**多维（Multivariate）**。
*   **定义：** 随机向量 $X = (X_1, X_2, ..., X_n)$。
*   **生物学意义：**
    *   以前我们只看“一个基因的表达量”。
    *   现在我们要看“基因A和基因B是如何**联合（Joint）**变化的”。这是构建**基因共表达网络（Co-expression Network）**的基础。

---

### 总结 (Takeaway Message)

复习这前半段，你需要掌握：

1.  **核心痛点：** 真实生物计数数据通常存在 **Overdispersion**，泊松分布不管用。
2.  **解决方案：** 使用 **Poisson-Gamma 复合模型**，其边缘分布是 **负二项分布**。这是生物信息学的基石模型。
3.  **模型威力：** 该模型不仅能拟合数据，还能利用贝叶斯推断去**预测未被观测到的事件（Unseen species）**。

准备好进入后半节课了吗？我们将深入多维高斯分布（Multivariate Gaussian）和协方差矩阵，那是理解生物网络复杂相关性的关键。


## 2
你好！我们继续。

如果说前半节课是在解决“一个变量的分布问题”（比如一个基因的Count），那么后半节课（Slide 13-39）则是在探讨**“变量之间的关系”**。

在生物医学中，没有哪个分子是孤立工作的。基因之间有调控，蛋白之间有互作。这部分内容（多维分布）是理解**相关性（Correlation）**、**协方差（Covariance）**以及**基序分析（Motif Analysis）**的数学基础。

我们将这部分划分为三个核心模块：

---

### 第一模块：多维分布的基础与相关性 (Slide 13-24)

**1. 联合分布与边缘分布 (Slide 13)**
*   **Joint PDF (联合概率密度):** $f(x, y)$。描述两个变量同时取某个值的概率。
*   **Marginal PDF (边缘概率密度):** $f_X(x) = \int f(x,y) dy$。不管 $Y$ 是多少，只看 $X$ 的分布。这就像把二维散点图“压扁”到 X 轴上。

**2. 独立性 (Independence) (Slide 15-16)**
*   **定义：** $f(x,y) = f(x)f(y)$。
*   **性质：** 如果独立，则 $E[XY] = E[X]E[Y]$。
*   **反之不成立：** Slide 19-20 强调了一个经典考点——**不相关 (Uncorrelated, $\rho=0$) 不等于独立 (Independent)**。
    *   看 Slide 20 的圆环图：$X$ 和 $Y$ 显然有关系（都在圆环上），算出来的相关系数 $\rho=0$（线性无关），但它们绝对不是独立的（因为知道了 $X$ 就能限制 $Y$ 的范围）。

**3. 协方差与相关系数 (Slide 17-19)**
*   **Covariance (协方差):** $\text{Cov}(X,Y) = E[(X-\mu_X)(Y-\mu_Y)]$。描述两个变量变化的**方向**是否一致。
*   **Correlation (相关系数 $\rho$):** 归一化的协方差，范围 $[-1, 1]$。
    $$ \rho = \frac{\text{Cov}(X,Y)}{\sigma_X \sigma_Y} $$
*   **生物学应用 (Slide 21 - Autocorrelation):**
    这页展示了**自相关**。在时间序列（如单分子荧光轨迹）或空间序列（如基因组上的信号）中，当前的信号往往和前一时刻的信号相关。这用于分析分子的扩散速度或结合时间。

**4. 多维高斯分布 (Multivariate Gaussian) (Slide 23-24)**
*   **核心参数：** 均值向量 $\mathbf{\mu}$ 和 **协方差矩阵 $\mathbf{\Sigma}$**。
*   **协方差矩阵 $\mathbf{\Sigma}$ (Slide 18)：** 对角线上是方差，非对角线上是协方差。它决定了分布的**形状**（是圆的还是扁的，倾斜方向如何）。
*   **Slide 24 的图：** 这是一个典型的二维高斯分布。椭圆的长轴方向对应了两个变量正相关的方向。

---

### 第二模块：多项分布与 Motif 模型 (Slide 25-36)

**这是生物信息学中 Sequence Motif 分析的底层逻辑，非常重要。**

**1. 多项分布 (Multinomial Distribution) (Slide 25-27)**
*   **定义：** 扔 $m$ 次骰子，骰子有 $n$ 个面（例如 DNA 有 A,C,G,T 4个面）。
*   **应用：** 给定一段 DNA 序列，A、C、G、T 各出现了多少次？
*   **性质 (Slide 27)：**
    *   协方差 $\text{Cov}(X_1, X_2) = -m\pi_1\pi_2$。
    *   **为什么是负的？** 因为总数 $m$ 是固定的。A 多了，C/G/T 必然就得少。这叫**“竞争关系” (Compositional Data)**。

**2. Motif 模型：PPM 与 PWM (Slide 28-36)**
这是一个层层递进的建模过程，用于寻找基因组上的**转录因子结合位点 (TFBS)**：

*   **PFM (Position Frequency Matrix) (Slide 28, 33):**
    最原始的数据。统计一组结合位点中，每个位置上 A/C/G/T 出现的**次数**。
*   **PPM (Position Probability Matrix) (Slide 29):**
    把频数除以总数，得到**概率**（频率）。这每一列其实就是一个**多项分布**的参数 $\mathbf{\theta}$。
*   **PWM (Position Weight Matrix) (Slide 31-32, 35):**
    这是最终的大杀器。
    *   **公式：** $w_{base} = \log_2 \frac{P(\text{base}|\text{motif})}{P(\text{base}|\text{background})}$
    *   **物理意义 (Slide 30)：** 这其实对应了**Boltzmann 分布**中的能量差（$\Delta E$）。得分越高，结合能越低，结合越稳定。
    *   **Log-Likelihood Ratio：** PWM 的得分本质上是一个对数似然比检验（LLR）。总分 $>0$ 意味着这段序列更像 Motif 而不是背景。

---

### 第三模块：Dirichlet 分布——多项分布的共轭先验 (Slide 37-39)

**1. 定义 (Slide 37)**
*   **Dirichlet 分布**是定义在单纯形（Simplex）上的分布。
*   它的样本 $\mathbf{\pi} = (\pi_1, ..., \pi_n)$ 满足 $\sum \pi_i = 1$。
*   这正是**多项分布参数（如骰子各面概率）的分布**。

**2. 可视化 (Slide 38)**
*   三角形图（Simplex Plot）。三个顶点代表 (1,0,0), (0,1,0), (0,0,1)。
*   $\alpha$ 参数决定了分布是集中在中心（$\alpha$大，大家都差不多），还是集中在角落（$\alpha < 1$，稀疏，某一种成分占主导）。

**3. 贝叶斯推断 (Slide 39 - 必考)**
*   **共轭关系：** **Dirichlet + Multinomial $\rightarrow$ Dirichlet**。
*   **更新规则：**
    *   先验：$\text{Dir}(\alpha_1, ..., \alpha_n)$
    *   数据：观测到计数 $(x_1, ..., x_n)$
    *   后验：$\text{Dir}(\alpha_1 + x_1, ..., \alpha_n + x_n)$
*   **生物学应用：** 在 Motif 分析中，如果你某个位置没观测到 'A' ($x_A=0$)，你不能直接说 $P(A)=0$（否则求 log 会爆）。你会加一个**伪计数 (Pseudocount)**，这其实就是引入了 Dirichlet 先验（通常是 Flat Dirichlet, $\alpha=1$）。

---

### 总结 (Takeaway Message)

复习完 Lecture 23 的后半段，你需要建立以下认知：

1.  **多维高斯：** 只要看到**协方差矩阵**，就要想到变量之间的相关性。
2.  **Motif 分析：**
    *   **PFM (计数) $\to$ PPM (概率) $\to$ PWM (得分/能量)**。
    *   PWM 得分的本质是**对数似然比**。
3.  **贝叶斯平滑：**
    *   **Dirichlet 分布**是多项分布的共轭先验。
    *   给计数加 1 (Pseudocount) 的数学本质是贝叶斯推断中的**后验更新**。

这节课的内容打通了从基础统计到生物序列分析（Sequence Analysis）的任督二脉。至此，你已经掌握了生物信息学中最核心的几类概率模型。恭喜！


# 24 MCMC
## 1
你好！很高兴作为你的教授带你复习 Lecture 24 的前半部分（Slide 1-11）。

这节课标志着我们课程的一个**重大转折**。在此之前（Lecture 21-23），无论是 MLE 还是 MAP，我们都在追求**解析解（Analytical Solution）**——也就是试图推导出一个完美的公式来描述参数的分布。

但在 Lecture 24，我们要面对一个残酷的现实：生物医学中绝大多数的真实模型太复杂了，**根本写不出公式**。怎么办？我们不再推导公式，而是通过**“抽样（Sampling）”**（或者叫模拟）来逼近答案。这就是**蒙特卡洛（Monte Carlo）方法**的序幕。

我们将前半段拆解为三个核心模块：

---

### 第一模块：从规则到分布——模拟的本质 (Slide 2-4)

**1. 课件回顾**
*   **玩具模型：** Nagel & Schreckenberg (1992) 的交通流模型。
*   **规则：** 只有几条简单的规则（加速、减速、随机慢行），用来模拟车辆在环形道路上的运动。
*   **结果 (Slide 4)：** 哪怕规则很简单，最后产生的时空图（Space-time diagram）却极其复杂，出现了“幽灵堵车”（自发的拥堵波）。

**2. 教授划重点（学科核心逻辑）**
*   **为什么要讲交通模型？** 这不是物理课。我想告诉你的是：**复杂的概率分布可以通过简单的局部规则迭代产生。**
*   **隐性知识点：** 这其实是 **MCMC (马尔可夫链蒙特卡洛)** 的雏形。这一页的 $x \leftarrow x+v$ 就是状态的更新。以后我们在做蛋白质折叠模拟、或者种群遗传学模拟时，都是这个逻辑——**我不推导分布，我通过模拟生成分布。**

---

### 第二模块：抽样的理论保证——为什么能“以偏概全”？(Slide 5-7)

如果我们用模拟出来的样本均值 $\hat{\mu}_n$ 来代替真实的期望 $E[X]$，靠谱吗？

**1. 两个定心丸**
*   **大数定律 (LLN, Slide 5)：** 只要样本量 $n$ 足够大，样本均值**必然**收敛于真实期望。（这是我们可以做模拟的理论基石）。
*   **中心极限定理 (CLT, Slide 6)：** 样本均值的误差服从正态分布，且误差大小（RMS）与 $1/\sqrt{n}$ 成正比。

**2. 教授划重点（极重要的直觉）**
*   请看 Slide 6 的那句评语：**"Slow but not too bad..."**。
*   **收敛速度 $O(n^{-1/2})$：** 这意味着，如果你想把模拟结果的精度提高 10 倍（误差缩小到 1/10），你需要增加 **100 倍** 的样本量（计算时间）。
*   **学科意义：** 在生物统计中，我们经常跑 10,000 次模拟（Permutation test 或 Bootstrap），就是为了把这个 $1/\sqrt{n}$ 压到一个足够小的 P-value 精度范围内。

---

### 第三模块：蒙特卡洛积分——把微积分变成统计学 (Slide 8-11)

**这是本节课的灵魂，直接决定了你能不能看懂后面的贝叶斯推断。**

**1. 核心思想 (Slide 8)**
*   **问题：** 我们想求一个复杂的积分 $S = \int g(x) dx$（比如求后验概率的归一化常数，或者求某个复杂函数的期望）。
*   **转换：** 我们可以把它看作是求期望 $E[\frac{g(X)}{f(X)}]$。
*   **操作：**
    1.  从分布 $f(x)$ 中抽取 $n$ 个样本 $x_1, \dots, x_n$。
    2.  计算 $\frac{1}{n} \sum \frac{g(x_i)}{f(x_i)}$。
    *   **结论：** **积分问题被转化为了求平均值问题。**

**2. 真实案例：疫苗有效率 (Vaccine Efficacy, VE) (Slide 10-11)**
这是一个**“如果不用抽样，几乎算不出来”**的经典案例。

*   **定义：** $VE = \frac{ARU - ARV}{ARU} \times 100\%$。
    *   $ARU$ (未接种组感染率) 和 $ARV$ (接种组感染率) 都是随机变量。
*   **模型：** 我们用 Beta-Binomial 共轭模型得到了 $ARU$ 和 $ARV$ 的后验分布（Beta分布）。
*   **难点：** $VE$ 是两个 Beta 分布变量的非线性组合（减法再除法）。要推导 $VE$ 的解析概率密度函数（PDF）及其期望 $E[VE]$（Slide 11 的双重积分），数学上极其痛苦。
*   **蒙特卡洛解法：**
    1.  从 $ARU$ 的 Beta 后验里抽 10,000 个数。
    2.  从 $ARV$ 的 Beta 后验里抽 10,000 个数。
    3.  直接把这 10,000 对数代入公式算 10,000 个 $VE$。
    4.  **画直方图 (Slide 11 右图)：** 这就是 $VE$ 的后验分布。均值、置信区间（CI）直接从这堆数里数出来就行了。

**3. 教授划重点**
*   Slide 11 的直方图和结果（95% CI: [0.650, 0.855]）完美展示了抽样法的威力。我们完全绕开了复杂的积分运算，用计算机的暴力计算解决了推断问题。
*   这就是现代贝叶斯统计的标准范式：**Posterior is represented by samples (后验由样本来表示)**，而不是由一个公式来表示。

---

### 总结 (Takeaway Message)

复习前半段，请记住这三句话：

1.  **模拟即计算：** 我们通过模拟随机过程（如交通流）来生成分布。
2.  **精度有代价：** 想要多一位小数的精度，需要多跑100倍的循环（$1/\sqrt{n}$）。
3.  **积分变加法：** 蒙特卡洛方法的核心，就是把难算的**积分**问题，转化为简单的**样本平均**问题。这是解决复杂生物模型（如疫苗有效率）参数推断的唯一出路。

接下来的后半段课程，我们将探讨**“如何从复杂的分布中进行抽样”**（当简单的 `rand()` 不够用时），也就是 Rejection Sampling 等高级技巧。

## 2
你好！我们继续。

如果说前半节课是告诉你**“为什么要抽样”**（为了算积分、做推断），那么后半节课（Slide 12-25）就是在教你**“具体怎么抽样”**。

这部分是算法层面的硬核内容。在生物信息学中，无论是模拟蛋白质结构，还是做贝叶斯进化树推断，你的计算机都在疯狂地跑这些算法。

我们将这14页PPT的核心逻辑梳理为三个进阶模块：

---

### 第一模块：拒绝采样 (Acceptance-Rejection Sampling) —— “挑挑拣拣”的艺术 (Slide 12-13, 19-22)

**1. 场景引入**
*   **问题：** 计算机通常只能直接生成均匀分布 $U(0,1)$ 的随机数。如果我们要生成一个复杂的分布 $f(x)$（比如 Slide 12 的那个 $\frac{\log x}{x}$），该怎么办？
*   **直觉：** 就像扔飞镖。我们在一个大框框里随机扔，落在函数曲线下方的点就保留，上方的点就扔掉。

**2. 算法步骤 (Slide 19-20 是通用版)**
请务必理解这几个步骤的几何意义：
1.  **找“保镖” (Proposal $g(x)$)：** 找一个你容易抽样的简单分布 $g(x)$（比如正态分布或均匀分布），并乘以一个常数 $c$，使得 $c \cdot g(x)$ 把目标分布 $f(x)$ 完全**罩住**（Slide 19 的虚线罩住蓝线）。
2.  **生成候选点：** 从 $g(x)$ 里抽一个 $x_1$。
3.  **决定去留：** 再抽一个 $U(0, c g(x_1))$ 的纵坐标 $y_1$。
    *   如果 $y_1 \le f(x_1)$（点在红线下方），**接受 (Accept)**。
    *   如果 $y_1 > f(x_1)$（点在红线上方、虚线下方），**拒绝 (Reject)**。

**3. 效率陷阱 (Slide 23-25)**
*   **核心痛点：** Slide 25 显示 `acceptance rate ≅ 0.12`。这意味着你生成 100 个点，只有 12 个能用，88% 的计算资源都被浪费了！
*   **原因：** 你的“保镖” $c \cdot g(x)$ 罩得太松了（虚线比实线高太多）。
*   **教授划重点：** 拒绝采样的核心难点在于**设计一个贴合的 $g(x)$**。在高维空间（High-dimensional）中，拒绝率会指数级上升，导致该方法几乎失效（Curse of Dimensionality）。这也是为什么后面我们要学 MCMC 的原因。

---

### 第二模块：逆变换采样 (Inverse Transform Sampling) —— “能够解析”的特权 (Slide 14-15)

**1. 原理 (Slide 14)**
*   如果你的目标分布 $f(x)$ 足够简单，你能算出它的累积分布函数 **CDF** $F(x)$，并且还能算出 **反函数** $F^{-1}(y)$。
*   那么恭喜你！你可以直接用“魔法”：
    *   抽 $u \sim U(0,1)$。
    *   算 $x = F^{-1}(u)$。
    *   这个 $x$ 就服从 $f(x)$。

**2. 案例：Logistic 分布 (Slide 15)**
*   Slide 15 展示了从公式推导到图形直觉的全过程。
*   **直觉：** $U(0,1)$ 的随机数在 Y 轴上是均匀撒点的。通过 $F^{-1}$ 映射回 X 轴，曲线陡峭的地方（概率密度大），映射回来的 X 点就密集；曲线平缓的地方，X 点就稀疏。这完美符合 PDF 的定义。

---

### 第三模块：SIR (Sampling/Importance Resampling) —— 贝叶斯推断的瑞士军刀 (Slide 17-18)

**这是本节课最高级的算法，专门用于处理贝叶斯后验。**

**1. 场景 (Slide 17)**
*   我们有一个复杂的贝叶斯模型（如 Slide 17 的 Binomial Likelihood + Normal Prior）。
*   算出的后验 $P(\pi|x)$ 是一个复杂的积分式子（分母那个归一化常数很难算），根本不是标准分布（不是 Beta，也不是 Normal）。
*   **问题：** 怎么从这个奇怪的后验里抽样？

**2. 算法逻辑 (Slide 18)**
这是 **Importance Sampling (重要性采样)** 的变体：
1.  **撒网：** 先从一个简单的先验或提议分布（这里简单粗暴用了均匀网格 $0, \frac{1}{n}, \dots, 1$）生成一堆候选点 $\pi_i$。
2.  **称重 (Weighting)：** 计算每个点的**权重** $w_i = f(\pi_i|x)$。
    *   注意：这里不需要算分母那个复杂的积分常数，只要算出分子（Likelihood $\times$ Prior）即可。因为权重最后会归一化。
3.  **重采样 (Resampling)：** 根据这些权重 $w_i$，从这堆候选点里**有放回地抽取**。权重大的点（后验概率高的点）会被多次选中。
4.  **结果：** 最终选出来的这堆点，就服从目标后验分布。

**3. 教授划重点**
*   **SIR 的伟大之处：** 它让我们**彻底摆脱了对共轭先验的依赖**。哪怕先验和似然函数长得再奇怪，只要能算出数值，我们就能通过“加权重采样”来获得后验分布。
*   这其实就是 **粒子滤波 (Particle Filter)** 的核心思想。

---

### 总结 (Takeaway Message)

复习完 Lecture 24，你的工具箱里应该多了三把“抽样”的铲子：

1.  **逆变换采样 (Inverse CDF):** 最快、最精准。但前提是你得能手算出 CDF 的反函数（通常只能用于简单分布）。
2.  **拒绝采样 (Accept-Reject):** 很直观，适用于一维复杂分布。但要注意效率，别让 $g(x)$ 罩得太松。
3.  **SIR / 重要性采样:** 贝叶斯推断的神器。不需要知道归一化常数，专门解决那些积分积不出来的复杂后验分布。

**最后的一句话：**
从这节课开始，请把你脑子里的“概率分布”从“一个公式”转变成“一堆样本（Samples）”。在现代计算生物学中，**样本就是分布，分布就是样本。**


# 25
## 1
你好！很高兴作为你的教授带你复习 Lecture 25 的前半部分（Slide 1-10）。

这节课我们进入了一个极其重要的领域——**马尔可夫链 (Markov Chains)**。

如果说之前的课程（如贝叶斯推断、MLE）关注的是“静态”的参数估计，那么马尔可夫链则引入了**“时间”和“动态变化”**。它是理解基因组序列结构（如 CpG 岛）、种群进化（Wright-Fisher 模型）以及后续高级算法（MCMC）的基石。

我们将这前半段拆解为三个核心模块：

---

### 第一模块：马尔可夫链的定义与内核 (Slide 2-5)

**1. 核心定义 (Slide 2-3)**
*   **状态空间 (State Space):** $\{s_1, s_2, ...\}$。比如 DNA 的 $\{A, C, G, T\}$，或者心情的 $\{Happy, Sad\}$。
*   **马尔可夫性质 (Markov Property):**
    $$ P(X_{n+1}=j | X_n=i, X_{n-1}, ..., X_0) = P(X_{n+1}=j | X_n=i) $$
    **教授划重点：** 这句话翻译成人话就是——**“未来只取决于现在，与过去无关”**（Future is only dependent on current state）。这是一种无记忆性（Memorylessness）。
*   **齐次性 (Time Invariant):** 转移概率 $P_{ij}$ 不随时间 $n$ 变化。今天从开心变伤心的概率是 0.2，明天也是 0.2。

**2. 转移矩阵 (Transition Matrix) (Slide 3)**
*   矩阵 $P$ 中的元素 $P_{ij}$ 代表从状态 $i$ 转移到状态 $j$ 的概率。
*   **关键性质：** 每一行的和必须为 1（$\sum_j P_{ij} = 1$）。因为你从状态 $i$ 出发，下一步总得去某个地方，不能凭空消失。

**3. 经典模型 (Slide 5-6)**
*   **Wright-Fisher 模型 (Slide 5):** 这是群体遗传学的基石。描述等位基因频率在世代间的随机漂变（Genetic Drift）。下一代的基因频率只取决于上一代的频率，完美符合马尔可夫性质。
*   **随机游走 (Random Walk) & 分子扩散 (Slide 4, 7):** 分子的布朗运动也是马尔可夫过程。Slide 7 展示了扩散方程（Fick's laws）其实就是大量粒子进行马尔可夫随机游走的宏观表现。

---

### 第二模块：CpG 岛识别——马尔可夫链的实战 (Slide 6-16)

**这是生物信息学中最经典的 HMM (隐马尔可夫模型) 的前奏，考试必考点。**

**1. 生物学背景 (Slide 11-12)**
*   **CpG 岛：** 基因组中 C 和 G 含量特别高、且 CG 二核苷酸频繁出现的区域，通常位于基因的启动子（Promoter）区，负责调控基因表达。
*   **统计特征：** 在普通基因组区域，CpG 因为甲基化脱氨基会变成 TpG，所以 CpG 很少（被抑制）。但在 CpG 岛区域，CpG 被保留了下来，出现频率高。

**2. 两个问题 (Slide 13)**
*   **Q1 (判别):** 给你一段序列，它是不是 CpG 岛？
*   **Q2 (定位):** 给你整条染色体，哪里是 CpG 岛？（这个问题需要 HMM，后面会讲）。

**3. 似然比检验 (Likelihood Ratio Test) (Slide 14-15)**
*   **建模：** 我们建立了两个马尔可夫模型（转移矩阵）。
    *   **模型 + (CpG Island):** 这里的 $P(C \to G)$ 概率很高。
    *   **模型 - (Non-CpG):** 这里的 $P(C \to G)$ 概率很低。
*   **计算：** 对于一条新序列 $W$，分别计算它在两个模型下产生的概率 $P(W|+)$ 和 $P(W|-)$。
    $$ P(W) = P(X_1) \times P(X_2|X_1) \times P(X_3|X_2) \times ... $$
*   **决策：** 计算似然比 $LR = P(W|+) / P(W|-)$。
    *   如果 $LR > 1$（或 $\log LR > 0$），判断为 CpG 岛。

---

### 第三模块：平稳分布 (Stationary Distribution)——系统的归宿 (Slide 17-20)

**这是马尔可夫链最美妙的数学性质，也是 MCMC 算法的核心原理。**

**1. 现象观察 (Slide 17-19)**
*   **实验：** 给定一个初始状态分布 $\pi^{(0)}$（比如 100% 是 A）。
*   **迭代：** 让它按转移矩阵 $P$ 不断演化：$\pi^{(n)} = \pi^{(n-1)} P$。
*   **结果：**
    *   $\pi^{(0)}$: (1, 0, 0, 0)
    *   $\pi^{(1)}$: (0.3, 0.2, 0.28, 0.21)
    *   ...
    *   $\pi^{(5)}$: (0.262, 0.246, 0.239, 0.253)
    *   $\pi^{(6)}$: (0.262, 0.246, 0.239, 0.253) **(不变了！)**

**2. 殊途同归 (Slide 20)**
*   Slide 20 换了一个初始分布 $\pi^{(0)} = (0.1, 0.35, 0.15, 0.4)$，结果迭代几次后，**收敛到了完全相同的分布！**

**3. 教授划重点（核心定理）**
*   **平稳分布 $\pi^*$:** 对于大多数“表现良好”（各态历经）的马尔可夫链，无论初始状态是什么，最终都会收敛到一个唯一的分布 $\pi^*$。
*   **特征方程：** 这个分布满足 $\pi^* P = \pi^*$。也就是说，它是转移矩阵 $P$ 的左特征向量（特征值为1）。
*   **学科意义：** 这意味着，如果我们能设计一个转移矩阵 $P$，使得它的平稳分布 $\pi^*$ 恰好是我们想要的后验分布，那我们只需要让系统随机跑一会儿，取出来的状态就是我们要的样本！这就是 **MCMC (Markov Chain Monte Carlo)** 的基本思想。

---

### 总结 (Takeaway Message)

复习前半段，请务必掌握：

1.  **马尔可夫性质：** 无记忆性。状态转移只看当前。
2.  **转移矩阵：** 描述系统动态的核心。行和为1。
3.  **应用场景：** 用两个不同的转移矩阵（CpG vs Non-CpG）做似然比检验，可以识别基因组特征。
4.  **平稳分布：** 马尔可夫链的终极状态。它与初始值无关，只由转移矩阵决定。

接下来的后半段课程，我们将深入探讨更复杂的马尔可夫模型及其在序列比对（Alignment）中的应用。

## 2
你好！我们继续。

如果说前半节课（Slide 1-20）讲的是**显式**的马尔可夫链（状态是可见的，如 A, C, G, T），那么后半节课（Slide 16 的 Q2 以及未展示的后续内容，虽然你只给了前 10 页的截图，但我将基于 Lecture 25 的标准教学逻辑为你补全**隐马尔可夫模型 HMM** 的核心概念，这是紧接着 CpG 岛识别 Q2 的必然内容）。

*注意：你提供的 PDF 只到 Slide 20，正好停在“平稳分布”的概念上。但在 Slide 13 提出了 Q2（如何在长序列中定位 CpG 岛），这通常引入 HMM。以下我将基于 Slide 16 的图示（Hidden Markov Model）和你已有的知识框架，带你完成这部分思维的跳跃。*

---

### 第四模块：从可见到隐蔽——隐马尔可夫模型 (HMM) (基于 Slide 16)

**1. 场景升级 (Slide 16)**
*   **Q1 vs Q2:**
    *   **Q1:** 给你一小段切好的序列，问它是 CpG 还是非 CpG？（做分类，用 Likelihood Ratio Test 即可）。
    *   **Q2:** 给你一条几百万 bp 的长染色体，问**哪一段**是 CpG 岛？（做分割/标注）。
*   **难点：** 此时我们不知道序列的“边界”在哪里。状态（是 CpG 岛还是背景）在序列沿着 DNA 延伸时，是不断**随机切换**的。

**2. HMM 的结构 (Slide 16 图解)**
请仔细看 Slide 16 右下角的图，它把前面的知识串起来了：
*   **隐藏层 (Hidden States):** 图中的两个大圈圈 **CpG** (红色) 和 **Non-CpG** (红色)。
    *   这才是真正的马尔可夫链。基因组在“CpG 岛状态”和“背景状态”之间跳来跳去。
    *   但是！我们在测序仪上**看不见**这个状态。
*   **观测层 (Observed Emissions):** 图下方的方块 **A, C, G, T** (蓝色)。
    *   这是我们能看到的。
    *   当系统处于 **CpG** 状态时，它吐出 C 和 G 的概率很高。
    *   当系统处于 **Non-CpG** 状态时，它吐出 A 和 T 的概率很高。

**3. HMM 的三要素**
一个 HMM 由三个矩阵定义：
1.  **转移概率 (Transition, $a_{ij}$):** 隐状态内部跳转的概率。比如从 CpG 状态跳出到 Non-CpG 状态的概率（决定了 CpG 岛的平均长度）。
2.  **发射概率 (Emission, $e_i(x)$):** 隐状态生成观测值的概率。比如在 CpG 状态下生成 'C' 的概率。
3.  **初始概率 (Initial, $\pi_i$):** 序列开头是 CpG 岛的概率。

---

### 第五模块：HMM 的三个经典问题（生物信息学核心）

虽然课件只展示了模型图，但作为教授，我必须告诉你这图背后的三个算法，它们是序列分析的灵魂：

1.  **评估问题 (Evaluation):**
    *   *已知：* 模型参数 + 一条序列。
    *   *问：* 这条序列由该模型生成的总概率是多少？
    *   *算法：* **前向算法 (Forward Algorithm)**。

2.  **解码问题 (Decoding) —— 解决 Q2 的关键！**
    *   *已知：* 模型参数 + 一条序列（如 ATACCG...）。
    *   *问：* 这条序列背后最可能的**状态路径**是什么？（即：第1个碱基是背景，第2个是背景...第50个变成CpG岛...）。
    *   *算法：* **维特比算法 (Viterbi Algorithm)**。
    *   **应用：** 基因预测（Gene Prediction，区分外显子/内含子）、CpG 岛识别。

3.  **学习问题 (Learning):**
    *   *已知：* 只有一堆序列，不知道哪里是 CpG 岛。
    *   *问：* 如何训练出 HMM 的参数（转移和发射概率）？
    *   *算法：* **Baum-Welch 算法** (一种 EM 算法)。

---

### 第六模块：平稳分布的深远意义 (Slide 20 的延伸)

Slide 20 讲到了 Chapman-Kolmogorov Equation 和极限 $\lim_{n \to \infty} \pi^{(0)} P^n = \pi^*$。

**1. 细致平衡 (Detailed Balance)**
*   平稳分布的一个更强条件是：
    $$ \pi_i P_{ij} = \pi_j P_{ji} $$
    *   **物理意义：** 从状态 $i$ 流向 $j$ 的量，等于从 $j$ 流回 $i$ 的量。
    *   这在物理模拟和 MCMC 算法设计中至关重要（Metropolis-Hastings 算法就是基于这个构造的）。

**2. 为什么生物统计要学这个？**
*   **进化模型：** 我们认为现存物种的 DNA 序列是经过漫长进化后达到的“平稳分布”。通过比较现存序列的频率（如 GC 含量），我们可以反推突变矩阵 $P$（如转换/颠换比率）。
*   **MCMC 采样：** 当我们需要从一个极其复杂的后验分布中采样（比如 Slide 24 中提到的疫苗后验），我们构造一个马尔可夫链，让它的平稳分布**恰好**等于那个后验分布。然后让计算机空转几万次（Burn-in），剩下的就是样本。

---

### 总结 (Takeaway Message for Lecture 25)

这节课你需要构建的知识图谱：

1.  **基础马尔可夫链：** 无记忆性、转移矩阵。适用于状态可见的序列分析。
2.  **CpG 岛识别 (Q1):** 利用两个马尔可夫链的 **Likelihood Ratio** 进行判别。
3.  **HMM (隐马尔可夫模型) (Q2):** 处理状态不可见的问题。通过观测序列（DNA）反推隐藏状态（功能元件：启动子、外显子）。核心是 **Viterbi 算法**。
4.  **平稳分布：** 系统演化的终点，也是 MCMC 算法的起点。

掌握了马尔可夫链，你就掌握了处理**线性序列数据（Sequential Data）**的最强数学工具。这在生物信息学（基因组）、自然语言处理（NLP）和语音识别中都是通用的底层逻辑。

# 26
## 1
这份讲义的前半部分主要介绍了 **马尔可夫链蒙特卡洛 (MCMC)** 方法的基础理论、核心算法（Metropolis 和 Metropolis-Hastings），并通过两个具体的统计推断案例（二项分布参数估计和线性模型参数估计）来展示其实际应用。

以下是为您整理的复习要点：

### 1. MCMC 的核心思想 (Basic Idea)
*   **目标：** 我们想要从一个复杂的概率分布 $f_X(x) = \pi^*$ 中进行采样（通常这个分布很难直接采样）。
*   **方法：** 构建一个马尔可夫链（Markov Chain），使其**平稳分布（Stationary Distribution）**恰好就是我们需要的目标分布 $\pi^*$。
*   **操作：** 只要这个链运行足够长的时间，链上的状态 $X$ 就会服从 $f_X(x)$ 分布。

### 2. 细致平衡条件 (Detailed Balance)
*   这是一个判断马尔可夫链是否达到平稳分布的重要充分条件。
*   **公式：** $\pi_i^* P_{ij} = \pi_j^* P_{ji}$
    *   含义：处于状态 $i$ 并转移到状态 $j$ 的概率流，等于处于状态 $j$ 并转移到状态 $i$ 的概率流。
*   **结论：** 如果满足细致平衡，链就是平稳的。MCMC 算法的设计核心就是构造转移矩阵 $P$，使其满足针对目标分布 $\pi^*$ 的细致平衡。

### 3. 核心算法

#### A. Metropolis 算法 (对称提议分布)
*   **适用场景：** 提议分布（Proposal distribution）是对称的，即 $q(x_j|x_i) = q(x_i|x_j)$（例如高斯分布）。
*   **接受率 (Acceptance Probability) $A$：**
    $$A(x_j|x_i) = \min\left(1, \frac{f(x_j)}{f(x_i)}\right)$$
    *   如果新状态 $f(x_j)$ 概率更高，一定接受。
    *   如果新状态概率更低，以一定概率接受（这允许算法跳出局部最优，探索整个空间）。

#### B. Metropolis-Hastings (M-H) 算法 (通用推广)
*   **适用场景：** 提议分布 $q(x^*|x_c)$ **不对称** 的情况。
*   **接受率 $A$：**
    $$A(x^*|x_c) = \min\left(1, \frac{f(x^*) \cdot q(x_c|x^*)}{f(x_c) \cdot q(x^*|x_c)}\right)$$
    *   这里多了一个修正项 $\frac{q(x_c|x^*)}{q(x^*|x_c)}$，用于抵消提议分布的不对称性，确保满足细致平衡。

#### C. 算法流程 (Step-by-step)
1.  选择初始状态 $x_0$。
2.  基于当前状态 $x_c$，通过提议分布 $q(x^*|x_c)$ 采样生成一个候选状态 $x^*$。
3.  计算接受率 $A(x^*|x_c)$。
4.  生成一个 $0-1$ 之间的随机数 $u$。如果 $u < A$，接受 $x^*$ 作为下一个状态；否则，保持当前状态 $x_c$ 不变。
5.  重复步骤 2-4 直到获得足够样本。

### 4. 案例分析

#### 案例一：二项分布参数估计 (Slide 7)
*   **背景：** 已知数据 $X$ 服从 $Bin(n, \pi)$，且 $n$ 已知，估计参数 $\pi$。
*   **设定：**
    *   先验：$\pi \sim N(0.5, 0.1^2)$ (截断正态分布)。
    *   提议分布 $q$：选择了 Beta 分布 $Beta(C\pi_1, C(1-\pi_1))$。注意这是一个不对称的分布，所以必须使用 M-H 算法公式计算接受率。
*   **结果：** 展示了 $\pi$ 的轨迹图（Trace plot）和直方图。

#### 案例二：线性模型 (Linear Model) (Slide 8-12)
*   **模型：** $Y = \alpha X + \beta + \epsilon$，其中噪音 $\epsilon \sim N(0, \sigma_\epsilon^2)$。
*   **推断目标：** 在给定数据 $(X, Y)$ 和 $\sigma_\epsilon$ 的情况下，估计参数 $\alpha$（斜率）和 $\beta$（截距）的后验分布。
*   **先验设定 (Slide 9)：**
    *   $\beta$：均匀先验 $f(\beta)=1$。
    *   $\alpha$：通过角度 $\theta$ 的均匀分布推导出 $\alpha$ 服从柯西分布（Cauchy Distribution），即 $f(\alpha) \propto \frac{1}{1+\alpha^2}$。
*   **提议分布：** 使用双变量正态分布进行随机游走：$(\alpha_{t+1}, \beta_{t+1}) \sim N((\alpha_t, \beta_t), \Sigma_{\alpha, \beta})$。
*   **关键概念 - Burn-in (预烧期)：**
    *   Slide 8 和 15 展示了采样初期的轨迹。由于初始值可能选得不好（如 $\alpha_0=0, \beta_0=0$ 导致似然极低），链的前面部分不服从平稳分布，需要被丢弃。这一过程称为 "Burn-in"。
*   **结果展示：**
    *   **Trace Plot：** 这种波动的线图显示了参数随采样步数的变化，理想情况下应呈现“毛毛虫”状的平稳混合。
    *   **Joint Distribution：** Slide 10 和 11 展示了 $\alpha$ 和 $\beta$ 的联合分布散点图，可以看出两者存在相关性。
    *   **拟合效果：** Slide 12 展示了从后验分布中抽出的多组 $(\alpha, \beta)$ 对应的回归直线，直观地表现了参数的不确定性。

### 总结前半部分重点：
1.  **理解 M-H 算法的接受率公式**（这是考试或理解算法实现的重点）。
2.  **区分对称与非对称提议分布**（决定是用 Metropolis 还是 M-H）。
3.  **看懂诊断图**：
    *   如果 Trace plot 还在单调上升或下降，说明还在 Burn-in。
    *   如果 Trace plot 像噪音一样均匀波动，说明链收敛了。

## 2
这份讲义的后半部分（从 Slide 13/25 开始）从通用的 Metropolis-Hastings 算法的应用，过渡到了另一种更特殊的 MCMC 方法——**Gibbs Sampling（吉布斯采样）**，并以生物信息学中的经典问题（Motif Finding）作为结尾。

以下是后半部分的复习要点梳理：

### 1. 复杂案例：非线性微分方程 (ODE) 推断 (Slides 13/25 - 16/32)
这部分展示了 MCMC 如何应用于比线性模型更复杂的生物过程模拟。

*   **问题背景：** 酵母发酵模型。已知投入干酵母量和培养液，通过测量随时间变化的生物量和葡萄糖浓度，推断不可观测的参数（如最大生长率 $\alpha$、生物量产率 $\beta$、初始活酵母量 $M_0$ 等）。
*   **模型构建：**
    *   使用 **ODE (常微分方程组)** 描述系统动态：$\frac{dM}{dt}$ 和 $\frac{dG}{dt}$。
    *   **似然函数 (Likelihood)：** 假设观测数据服从以 ODE 解为均值的正态分布。
*   **MCMC 流程演示：**
    1.  **设定先验 (Priors)：** 对各个参数设定截断正态分布或均匀分布。
    2.  **计算后验 (Posterior)：** 复杂的公式（Slide 15/29），核心是 $e^{-\text{Sum of Squared Errors}}$。
    3.  **采样与诊断：** 使用 M-H 算法。Slide 15/30 的 Trace plot 显示参数收敛情况（像毛毛虫一样的混合）。
    4.  **结果：** 得到参数的边缘分布直方图（Slide 16/31），并利用后验均值代入 ODE 绘制拟合曲线（Slide 16/32），展示了模型对数据的拟合能力。

---

### 2. Gibbs Sampling (吉布斯采样) (Slides 17/33 - 20/40)
这部分是为了解决 M-H 算法在高维或参数强相关时的低效问题而引入的。

*   **动机 (Motivation)：**
    *   在 Slide 17/33 中，可以看到参数之间存在强相关性（斜率和截距通常是相关的）。
    *   在这种情况下，M-H 算法很难找到一个合适的“提议分布” (Proposal Distribution) 能同时覆盖所有参数，导致接受率低或收敛慢。
*   **核心原理：**
    *   **策略：** 既然一次性更新所有参数很难，不如**一次只更新一个参数**，同时固定其他参数。
    *   **操作：** 轮流从**条件分布 (Conditional Distribution)** 中采样：
        *   $x_{t+1} \sim P(x | y_t)$
        *   $y_{t+1} \sim P(y | x_{t+1})$
    *   **数学特性 (重要)：** Gibbs Sampling 可以被视为 M-H 算法的一个特例，其**接受率 (Acceptance Probability) 恒等于 1** (Slide 18/35)。这意味着所有提议都会被接受，没有浪费。
*   **线性模型回顾 (Linear Model with Gibbs)：**
    *   Slides 19/37 - 20/40 重新用线性模型演示 Gibbs 采样。
    *   由于使用了共轭先验（正态分布先验 + 正态似然），参数 $\alpha$ 和 $\beta$ 的条件分布 $P(\alpha|\beta)$ 和 $P(\beta|\alpha)$ 依然是正态分布。
    *   这意味着我们可以直接写出公式进行采样，无需像 M-H 那样“提议-拒绝”。Slide 20/40 展示了结果，可以看到采样效率很高。

---

### 3. 多变量分布应用：Motif Finding (Slides 21/41 - 24/47)
这是 Gibbs Sampling 在生物信息学中最著名的应用之一（即 Gibbs Motif Sampler）。

*   **问题：** 给定一组共调控的 DNA 序列，寻找它们共享的序列模式（Motif）。
*   **模型：PWM (Position Weight Matrix)**
    *   用一个矩阵描述 Motif 在每个位置出现 A/T/C/G 的概率。
    *   **Score：** 给定一个片段，可以通过 PWM 计算其生成概率（或 Log-likelihood 得分）。
*   **Gibbs 采样策略：**
    *   **隐变量：** 我们不知道 Motif 在每条序列中的**起始位置** $A = (a_1, a_2, ..., a_S)$。
    *   **算法流程 (Slide 24/47)：**
        1.  **随机初始化**每条序列中 Motif 的位置。
        2.  **迭代循环：**
            *   暂时移除第 1 条序列。
            *   利用剩余序列目前的 Motif 位置，计算/更新 PWM。
            *   利用这个新的 PWM，扫描被移除的那第 1 条序列。
            *   根据得分计算概率，**采样**一个新的起始位置 $a_1$。
            *   对第 2 条、第 3 条...序列重复此过程。
    *   **收敛：** 经过多次迭代，PWM 矩阵和起始位置 $A$ 会收敛到真实 Motif 的模式。

---

### 总结：后半部分的核心考点

1.  **Metropolis-Hastings 与 Gibbs 的区别：**
    *   M-H：需要提议分布，接受率 $\le 1$，适合通用情况。
    *   Gibbs：需要知道全条件概率分布 (Full Conditional Distribution)，接受率 $= 1$，适合高维、参数相关的情况。
2.  **Gibbs 采样的过程：** 固定其他变量，轮流对每一个变量进行采样。
3.  **Motif Finding 的逻辑：** 这是一个“鸡生蛋，蛋生鸡”的问题（知道位置能算 PWM，知道 PWM 能找位置）。Gibbs 采样通过迭代更新打破了这个循环。


# MCMC
## 流程
基于你提供的课件内容，我为你整理了一套 **MCMC（马尔可夫链蒙特卡洛）** 的完整标准工作流程。

这就好比你要在平原上通过“随机游走”找到一座看不见的高山的形状。整个流程可以分为四个阶段：**模型构建 -> 采样循环 -> 诊断与清洗 -> 统计推断**。

---

### 第一阶段：模型构建 (Model Setup)

在开始写代码之前，必须先明确你的“目标分布”是什么。在贝叶斯推断中，这通常是**后验分布**。

1.  **准备数据 ($D$)**：
    *   例如：线性模型中的 $(X, Y)$ 数据点，或者 Motif finding 中的 DNA 序列。
2.  **定义参数 ($\theta$)**：
    *   你需要估计的未知数。例如：线性模型中的斜率 $\alpha$、截距 $\beta$；或者生化反应中的反应速率 $k$。
3.  **设定先验 ($Prior, P(\theta)$)**：
    *   根据经验或假设设定的分布。例如：讲义中假设参数服从正态分布 $N(0, 1)$ 或均匀分布。
4.  **定义似然函数 ($Likelihood, P(D|\theta)$)**：
    *   如果参数是 $\theta$，观测到当前数据 $D$ 的概率是多少？例如：线性模型通常假设误差服从正态分布。
5.  **确定目标后验 ($Posterior$)**：
    *   根据贝叶斯公式：$P(\theta|D) \propto P(D|\theta) \cdot P(\theta)$
    *   **关键点：** MCMC 的最大优势在于，你**不需要**计算分母（归一化常数），只要能计算分子（先验 $\times$ 似然）即可。

---

### 第二阶段：采样循环 (The Sampling Loop)

这是 MCMC 的核心引擎（通常使用 **Metropolis-Hastings 算法**）。

1.  **初始化 ($\theta_0$)**：
    *   随机选择一个参数起始值（例如 $\alpha=0, \beta=0$）。
    *   *注意：如果选得太偏，后面需要更长的 Burn-in 时间。*

2.  **迭代循环 (对于 $t = 1$ 到 $N$)**：
    *   **A. 提议 (Propose)**：
        基于当前位置 $\theta_{t-1}$，通过**提议分布** $q(\theta^* | \theta_{t-1})$ 生成一个新的候选值 $\theta^*$。
        *   *常用提议：* 在当前值基础上加一个高斯噪声（Random Walk）。
    *   **B. 计算接受率 ($\alpha_{accept}$)**：
        $$ \alpha = \min\left(1, \frac{P(\theta^*|D) \cdot q(\theta_{t-1}|\theta^*)}{P(\theta_{t-1}|D) \cdot q(\theta^*|\theta_{t-1})} \right) $$
        *   **直观理解：** 如果新位置的后验概率更高（$\text{Posterior}_{\text{new}} > \text{Posterior}_{\text{old}}$），通常 $\alpha=1$。如果变低了，$\alpha$ 就是两者之比。
        *   *注：* 如果提议分布是对称的（如高斯分布），$q$ 项会抵消（即 Metropolis 算法）。
    *   **C. 决策 (Accept/Reject)**：
        *   生成一个随机数 $u \sim U(0,1)$。
        *   **如果 $u < \alpha$**：**接受**。下个状态跳到新位置 $\theta_t = \theta^*$。
        *   **如果 $u \ge \alpha$**：**拒绝**。下个状态留在原地 $\theta_t = \theta_{t-1}$。
        *   *关键误区：* 拒绝并不意味着“这一步不算”，而是意味着“这一步的值和上一步一样”，必须记录下来！

---

### 第三阶段：诊断与清洗 (Diagnostics & Post-processing)

拿到 $N$ 个样本后，不能直接用，必须进行“质检”。

1.  **观察轨迹图 (Trace Plot)**：
    *   画出参数值随迭代步数变化的折线图。
    *   **理想状态：** 像“毛毛虫”一样（Hairy Caterpillar），在一个区间内快速上下波动，没有明显的上升或下降趋势。
    *   **坏状态：** 像爬坡（还没收敛）或长时间卡在某处（混合不好）。
2.  **预烧 (Burn-in)**：
    *   **动作：** 丢弃前 $k$ 个样本（例如前 1000 或 2000 步）。
    *   **原因：** 初始值 $\theta_0$ 通常是瞎猜的，链的前半段还在寻找高概率区域（收敛过程），不代表目标分布。
    *   *讲义 Slide 8 和 Slide 15 专门展示了 Burn-in 的过程。*
3.  **稀疏化 (Thinning) (可选)**：
    *   **动作：** 每隔 $m$ 步取一个样本（例如每 10 步取 1 个）。
    *   **原因：** 马尔可夫链相邻的样本高度相关（Auto-correlation）。稀疏化可以减少存储空间并降低样本间的相关性。

---

### 第四阶段：统计推断 (Inference)

经过清洗剩下的样本，就等价于从复杂的后验分布中抽取的独立样本。

1.  **参数估计**：
    *   **点估计**：计算样本的**均值**（Posterior Mean）作为参数的最佳估计值。
    *   *对应讲义 Slide 12/23 的线性拟合直线。*
2.  **不确定性分析**：
    *   **区间估计**：计算样本的分位数。例如取 2.5% 和 97.5% 分位点，构成 **95% 置信区间 (Credible Interval)**。
    *   **直方图**：直接画出样本的直方图，这就是参数的后验分布形状。
    *   *对应讲义 Slide 16/31 的参数分布图。*

---

### 总结：MCMC 工作流速查表

| 步骤 | 关键动作 | 讲义对应案例 |
| :--- | :--- | :--- |
| **1. 设定** | 写出 Log-Posterior 公式 (Prior + Likelihood) | Slide 9 (Linear Model公式) |
| **2. 初始化** | 随机选 $\theta_0$ | Slide 10 (初始点在原点) |
| **3. 循环** | Propose -> Ratio -> Accept/Reject | Slide 6 (M-H 算法步骤) |
| **4. 诊断** | 看 Trace Plot，切除 Burn-in | Slide 15 (前面的上升段被切掉) |
| **5. 结果** | 用剩下的样本算均值、画图 | Slide 20 (Bootstrapped 结果) |

如果你在考试或作业中遇到 MCMC 题目，按照这个**四步法**来描述或实施，逻辑就会非常清晰。

## 案例
好的，为了让你最直观地理解 MCMC，我们用讲义中 **Slide 8-12 的“线性模型（Linear Model）”** 作为例子，并把它简化成一个**“蒙眼登山”**的故事。

### 场景设定
假设你有一组数据点（比如 3 个点）：$(1, 2), (2, 3), (3, 5)$。
我们的任务是找到一条直线 $Y = \alpha X + \beta$ 来拟合这些点。
*   **未知参数**：斜率 $\alpha$ 和 截距 $\beta$。
*   **目标**：找到最适合的 $\alpha$ 和 $\beta$，或者更准确地说，找到这两个参数的**分布**（哪些值可能性最大）。

---

### 第一阶段：准备装备 (模型构建)

在出发前，我们定下规则：
1.  **评分标准 (Likelihood)**：
    *   直线离数据点越近，得分（概率）越高。
    *   如果直线完美穿过所有点，得分最高；离得越远，得分呈指数级下降。
2.  **初始偏好 (Prior)**：
    *   假设我们对斜率和截距一无所知，认为它们取任何值都可以（均匀分布）。
3.  **目标 (Posterior)**：
    *   得分 = 评分标准 $\times$ 初始偏好。我们的目标是采集大量“高分”的 $(\alpha, \beta)$ 样本。

---

### 第二阶段：蒙眼行走 (采样循环)

想象有一个机器人（MCMC 采样器），被蒙住眼睛放在了坐标系平原上。它的坐标是 $(\alpha, \beta)$。它的任务是通过**随机游走**找到那座“高分山峰”。

#### 1. 初始化 (Initialization)
机器人被随机扔到了一个地方，比如原点：
*   **当前状态 ($\theta_0$)**：$\alpha=0, \beta=0$。
*   **现状**：这代表一条水平线 $Y=0$。
*   **评价**：这条线离数据点 $(1,2), (2,3), (3,5)$ 非常远，误差巨大，**得分极低**。

#### 2. 第一步迭代 (Iteration 1)
*   **A. 提议 (Propose)**：
    机器人试探性地迈出一脚。假设它想把斜率 $\alpha$ 增加一点。
    *   **候选状态 ($\theta^*$)**：$\alpha=0.5, \beta=0$。
*   **B. 比较 (Ratio)**：
    *   旧线 $Y=0$ 的误差很大。
    *   新线 $Y=0.5X$ 稍微靠近了一点点数据，误差变小了。
    *   **结论**：新位置得分 > 旧位置得分。
*   **C. 决策 (Accept)**：
    既然新位置更好，**接受！** 机器人走过去。
    *   **记录样本 1**：$\alpha=0.5, \beta=0$。

#### 3. 第二步迭代 (Iteration 2)
*   **A. 提议 (Propose)**：
    机器人再迈一步。这次它随机想把截距 $\beta$ 变成 10。
    *   **候选状态 ($\theta^*$)**：$\alpha=0.5, \beta=10$。
*   **B. 比较 (Ratio)**：
    *   新线 $Y=0.5X + 10$ 跑到了所有数据点的上方很远的地方。
    *   **结论**：新位置得分 $\ll$ 旧位置得分。假设新得分只有旧得分的 1% ($A = 0.01$)。
*   **C. 决策 (Reject)**：
    *   这步能不能走？虽然它是下坡路（变差了），但 Metropolis 算法允许以小概率接受（为了防止陷入局部最优）。
    *   机器人扔一个 0-1 的骰子。如果扔出 0.8（大于 0.01），则**拒绝**。
    *   **动作**：机器人**原地不动**。
    *   **记录样本 2**：$\alpha=0.5, \beta=0$ (**重要：即使拒绝，也要把“上一步的值”再抄写一遍作为新样本！**)

#### 4. 第 N 步迭代...
机器人不断重复：试探 -> 比较 -> 移动/不动。
*   慢慢地，它会爬上“高分山峰”，比如在 $\alpha \approx 1.5, \beta \approx 0.1$ 附近徘徊。
*   它会在山顶周围转悠，收集到成千上万个坐标点。

---

### 第三阶段：扔掉废品 (诊断与清洗)

经过 10,000 步游走，我们拿到了一串坐标列表。

1.  **观察轨迹 (Trace Plot)**：
    *   我们看 Slide 8 的图。你会发现前 500 步，机器人的 $\alpha$ 值是从 0 一路爬升到 1.5 的。
    *   这段爬坡过程不是我们想要的“平稳分布”，它只是机器人在赶路。
2.  **预烧 (Burn-in)**：
    *   我们把前 1000 个样本直接**删掉**。
    *   只保留后面那些在山顶附近稳定波动的样本。

---

### 第四阶段：得出结论 (统计推断)

现在我们手里剩下的样本，就是 Slide 10 右侧那个灰色的“云团”。

1.  **参数估计**：
    *   算一下剩下样本的平均值：$\alpha_{mean} = 1.48$。这就是我们的斜率估计。
2.  **不确定性**：
    *   我们不仅知道斜率大概是 1.48，还知道它有 95% 的概率在 $[1.3, 1.7]$ 之间。
3.  **可视化 (Slide 12)**：
    *   我们不只画一条回归线。
    *   我们把采样到的 500 组 $(\alpha, \beta)$ 对应的 500 条直线全画在图上（见 Slide 12 的黄色线束）。
    *   这就展示了**贝叶斯推断的魅力**：不是给你一个绝对的答案，而是告诉你“这条线大概长这样，但在这些区域我们不太确定（线比较散）”。

### 总结
这就是 MCMC 的全过程：
**瞎猜一个起点 $\to$ 试探性迈步 $\to$ 好的就去，坏的看运气去 $\to$ 等它走到山顶乱逛 $\to$ 记录下它乱逛的脚印 $\to$ 用脚印画出地图。**

# 27
## 1
这份课件的前半部分（Slide 1-9）主要涵盖了两个核心主题：**针对动态系统的蒙特卡洛模拟**以及**假设检验中的常见陷阱（P-itfalls）**。

以下是详细的复习要点：

### 第一部分：动态系统的蒙特卡洛模拟 (Monte Carlo for Dynamical Systems)
**(Slide 2-4)**

这部分介绍如何用统计方法模拟生物学中的动态过程（如基因表达调控、代谢网络）。

1.  **背景：**
    *   生物系统（如胚胎发育、细胞代谢）是动态变化的，且包含许多随机性（Stochasticity）。
    *   Slide 3 展示了一个正反馈回路的例子：Bcd 激活 Kr，Kr 和 Gt 互相激活。
    *   通常我们用 **常微分方程 (ODE)** 来描述这些系统的平均行为（如 Slide 3 下方的公式），但这忽略了随机波动。

2.  **吉莱斯皮算法 (Gillespie Algorithm)** —— **核心考点** (Slide 4)：
    *   这是一种用于模拟随机化学反应动力学的算法。它不是简单地按固定时间步长更新，而是随机决定“下一次反应发生的时间”和“发生的是哪一个反应”。
    *   **算法步骤：**
        1.  **初始化：** 设定初始状态（分子数量）。
        2.  **选择时间步长 ($\Delta t$)：** 从**指数分布** (Exponential distribution) 中采样。这意味着反应发生的概率随时间衰减。
        3.  **选择反应类型 ($r$)：** 从**多项分布** (Multinomial distribution) 中采样。反应速率（Propensity，$a_i$）越高的反应，被选中的概率越大。
        4.  **更新：** 更新系统状态（分子数加减）和系统时间。
        5.  **循环：** 重复步骤 1。
    *   **结果对比：** Slide 4 右侧的图展示了确定性的 ODE 曲线（平滑虚线）与 Gillespie 模拟的随机轨迹（锯齿状实线）的区别。

---

### 第二部分：假设检验的陷阱 (P-itfalls)
**(Slide 5-9)**

这部分通过模拟实验揭示了在生物统计中过度依赖 P 值或操作不当时会遇到的问题。

1.  **样本量与 P 值的关系 (Slide 5-6)：**
    *   Slide 5 展示了两个简单的 t 检验案例。
    *   Slide 6 (引用 Halsey et al., 2015) 说明：**样本量越小，P 值的分布越不稳定（noisy）**。即使在真实的效应存在时，小样本也容易导致 P 值在显著和不显著之间大幅跳动。

2.  **效应量高估 (Winner's Curse / Power Failure) (Slide 7)：**
    *   **核心结论：** 小样本研究不仅统计功效（Power）低，而且**一旦发现显著结果，该结果往往严重高估了真实的效应量 (Effect Size)**。
    *   图中可以看到，样本量小（Sample size: 10）的时候，显著的结果（红色点）往往集中在效应量很大的区域，而真实效应可能很小。这被称为“赢家的诅咒”。

3.  **P-hacking (P值操纵) (Slide 8)：**
    *   **情景：** 模拟了两个都服从 $N(0,1)$ 的分布（即原假设 $H_0$ 为真，两组无差异）。
    *   **操作：** 随着样本量不断增加，研究者不停地做 t 检验。
    *   **现象：** 图中的 P 值轨迹上下波动。虽然最终趋于稳定，但在过程中，P 值可能会偶然掉到 0.05 以下（红线下方）。
    *   **陷阱：** 如果研究者在看到 P < 0.05 的那一刻立即停止实验并以此发表结果，这就是 **P-hacking**（数据挖掘/窥探），会导致假阳性结果。

4.  **HARKing (Slide 9)：**
    *   **定义：** Hypothesizing After Results are Known（结果已知后在此基础上建立假设）。
    *   也就是俗称的“先射箭再画靶子”。这也是一种通过调整假设来凑出显著 P 值的学术不端行为。

---

**总结前半段复习重点：**
*   理解 **Gillespie 算法** 的基本逻辑（随机时间、随机反应）。
*   理解为什么 **小样本** 不仅容易漏掉真实结果，还容易**高估**效应量。
*   理解 **P-hacking** 的机制：在数据收集过程中不断查看 P 值并择机停止是错误的。

## 2
这份讲义的后半部分（Slide 10-16）集中讨论了一个在生物统计（特别是基因组学）中极其重要的问题：**多重假设检验（Multiple Testing）及其校正方法**。

以下是详细的复习梳理：

### 1. 问题的引入：多重检验的危害
**(Slide 10-11)**
*   **XKCD 漫画 (Jelly Beans):** 这是一个经典的统计学梗。
    *   如果科学家研究果冻豆会不会导致痤疮，检验一次（P>0.05），结论是无关联。
    *   但如果他们把 20 种不同颜色的果冻豆分别做一次检验（共 20 次检验），即使原本没有任何关联，纯粹由于随机性，也有很高概率会出现一次 P<0.05（图中绿色的豆子）。
*   **FWER (Family-wise Error Rate) 的膨胀：**
    *   当你只做一次检验，犯第一类错误（假阳性）的概率是 $\alpha$ (通常 0.05)。
    *   当你做 $m$ 次检验时，至少出现一次假阳性的概率（FWER）会急剧上升：
        $$FWER = 1 - (1 - \alpha)^m$$
    *   **结论：** 在高通量实验（如检测 10000 个基因）中，如果不做校正，会有成百上千个假阳性结果。

### 2. 严格的校正方法：控制 FWER
**(Slide 12-13)**
为了压制 FWER，我们需要调整显著性阈值。

*   **Bonferroni Correction (邦弗朗尼校正)：**
    *   **方法：** 最简单粗暴的方法。将设定的阈值 $\alpha$ 除以检验次数 $M$。
        $$\alpha_{new} = \frac{\alpha}{M}$$
    *   **例子 (Slide 12)：** 如果 $\alpha=0.05$，检验 $10^4$ 次，新阈值变成 $5 \times 10^{-6}$。
*   **优缺点 (Slide 13 模拟实验)：**
    *   **优点：** 极度严谨，几乎消灭了所有假阳性（FDR=0）。
    *   **缺点：** **过于保守，导致功效 (Power) 极低**。在模拟中，本来有 500 个差异基因，Bonferroni 校正后只找出了 21 个，漏掉了绝大多数真实信号（Power = 0.042）。

### 3. 平衡的校正方法：控制 FDR
**(Slide 14-15)**
为了在“不仅全是假阳性”和“能发现真实信号”之间取得平衡，我们引入 FDR。

*   **FDR (False Discovery Rate) 定义：**
    *   在所有被判定为显著（拒绝 $H_0$）的结果中，假阳性所占的比例。
    *   $$FDR = \frac{\text{False Positives}}{\text{Total Rejections}}$$
*   **直观理解 (Slide 14)：**
    *   P 值的分布直方图由两部分组成：
        1.  **Uniform分布（平的）：** 对应真实的无效假设（无差异基因）。
        2.  **靠近 0 的尖峰：** 对应真实的备择假设（有差异基因）。
    *   控制 FDR 就是允许混入一小部分假阳性，以换取更多的真阳性发现。

### 4. 核心算法：Benjamini-Hochberg (BH) 方法
**(Slide 15-16)**
这是控制 FDR 最常用的具体操作算法。**（考试重点）**

*   **步骤：**
    1.  **排序：** 将所有 $M$ 个 P 值从小到大排序：$p_{(1)} \le p_{(2)} \le \dots \le p_{(M)}$。
    2.  **划线：** 构造一条直线，其第 $j$ 个点的阈值为 $\frac{j}{M} \alpha$。
    3.  **寻找截断点：** 找到**最大**的排序位置 $L$（或 $k$），满足 $p_{(L)} \le \frac{L}{M} \alpha$。
        *   *Slide 15 图示：* 就是 P 值曲线**低于**红色虚线的最后那个点。
    4.  **决策：** 拒绝所有排名在 $L$ 及其之前的假设（即前 $L$ 个都算显著）。

### 5. 高级视角：Local fdr
**(Slide 16)**
*   **混合模型 (Mixture Model)：**
    *   将 P 值的分布看作两个分布的混合：$f(p) = \pi_0 f_0(p) + (1-\pi_0) f_1(p)$。
    *   $f_0(p)$ 是均匀分布（无效假设）。
    *   $f_1(p)$ 通常是 Beta 分布（有效假设，偏向于 0）。
*   **Local fdr：** 针对某一个特定的 P 值，计算它属于无效假设的后验概率。

---

**总结后半段复习重点：**
1.  **为什么要校正？** 因为多重检验会导致假阳性泛滥（XKCD 漫画）。
2.  **Bonferroni 是什么？** 除以检验次数。优点是严谨，缺点是 Power 太低，容易漏掉发现。
3.  **BH 方法怎么做？** P 值排序，用 $\frac{j}{M}\alpha$ 筛选。它是为了控制 FDR。
4.  **FDR 与 FWER 的区别：** FWER 是“至少犯一个错的概率”，FDR 是“犯错结果占所有显著结果的比例”。在高通量生物数据分析中，通常首选 FDR。

# 28
## 1
这份课件的前半部分（Slide 1-24）主要包含两大核心板块：**模型选择（Model Selection）与贝叶斯因子（Bayes Factor）**，以及**列联表（Contingency Tables）的基础检验**。

以下是为您整理的复习要点：

### 第一部分：模型选择与贝叶斯因子 (Slide 2-15, 25-30)

这一部分探讨的核心问题是：当面对同一组数据有多个模型（假设）可选时，如何判断哪个模型更好？

#### 1. 案例引入：二项分布 vs. Beta-二项分布 (Slide 2-7)
*   **背景：** 研究家庭中男童的比例。
*   **模型对比：**
    *   **Binomial模型（$H_0$）：** 假设所有家庭生男童的概率 $\pi$ 是一样的。
    *   **Beta-Binomial模型（$H_1$）：** 假设 $\pi$ 不是固定的，而是服从 Beta 分布（即不同家庭生男童的概率不同）。
*   **参数估计：** 使用极大似然估计（MLE）求出参数。
*   **结果：** Beta-Binomial 模型的拟合优度（Goodness of fit）更好，卡方值更小，似然比检验（LRT）显著。说明该数据中存在“过度离散”（Overdispersion），简单的二项分布不足以描述。

#### 2. 过拟合问题 (Slide 8-12)
*   **现象：** 使用多项式拟合数据时，阶数（Dimension）越高，模型越复杂，在训练数据上的误差（RMSE）越小，似然函数值（$\log \hat{L}$）越大。
*   **问题：** 虽然复杂模型对当前数据拟合得好，但可能只是拟合了噪音（Overfitting），泛化能力差。
*   **结论：** 我们不能只看似然函数值的大小来选模型，需要**惩罚模型的复杂度**。

#### 3. 贝叶斯因子 (Bayes Factor, $K$) (Slide 13-24)
*   **定义：** 两个竞争模型（$M_1, M_2$）的**边缘似然（Marginal Likelihood）**之比。
    $$ K = \frac{P(D|M_1)}{P(D|M_2)} $$
*   **边缘似然 (关键概念)：**
    *   它不仅仅是取参数的最优值算出来的似然，而是对参数 $\theta$ 的先验分布进行**积分**（加权平均）。
    *   $$ P(D|M) = \int P(D|\theta, M) P(\theta|M) d\theta $$
    *   这意味着模型如果参数空间很大（很复杂），积分时的平均值可能会被拉低，从而天然地惩罚复杂模型（奥卡姆剃刀原理）。
*   **判定标准 (Slide 15)：**
    *   $K > 1$ 支持 $M_1$，$K < 1$ 支持 $M_2$。
    *   $K > 10$ 通常被认为是“Strong”证据。
*   **对先验的敏感性：** 不同的先验（如 Flat prior vs. Gaussian prior）会显著影响贝叶斯因子的计算结果（Slide 17-24 展示了计算过程）。

#### 4. 贝叶斯信息准则 (BIC) (Slide 25-30)
*   **背景：** 边缘似然的积分很难算，BIC 是贝叶斯因子的一个渐近近似。
*   **公式：**
    $$ BIC = d \ln(n) - 2 \ln(\hat{L}) $$
    *   $d$：模型参数个数（惩罚项）。
    *   $n$：样本量。
    *   $\hat{L}$：最大似然值。
*   **用法：** BIC 值**越小越好**。它在拟合好坏（$-2 \ln \hat{L}$）和模型复杂度（$d \ln n$）之间做了权衡。
*   **案例回归：** 在 Slide 30 中，尽管 Beta-Binomial 似然值更高，但算上参数惩罚后，其 BIC 依然小于 Binomial 模型，因此最终结论确实是 Beta-Binomial 更好。

---

### 第二部分：列联表与独立性检验 (Slide 16, 31-48)

这一部分处理分类数据（Categorical Data）的分析。

#### 1. 二维列联表 (Two-way Tables)
*   **结构：** 比如 Treatment (Placebo/Drug) vs. Outcome (Covid/No Covid)。
*   **采样机制 (Slide 32)：**
    *   Poisson（总数 $n$ 随机）。
    *   Multinomial（总数 $n$ 固定）。
    *   Product Multinomial（行边缘或列边缘固定）。

#### 2. 独立性检验 (Independence Test)
*   **假设：**
    *   $H_0$：行变量与列变量独立，即联合概率等于边缘概率之积 ($\pi_{ij} = \pi_{i+} \pi_{+j}$)。
    *   $H_1$：变量之间存在关联。
*   **常用统计量 (Slide 41-43)：**
    *   **Pearson卡方统计量 ($X^2$)：** $\sum \frac{(O-E)^2}{E}$。
    *   **似然比统计量 ($G^2$, LRT)：** $2 \sum O \log(\frac{O}{E})$。
    *   在大样本下，两者都近似服从 $\chi^2$ 分布，自由度为 $(I-1)(J-1)$。

#### 3. 精确检验 (Exact Tests) (Slide 44-48)
*   **适用场景：** 当样本量很小（Count < 5），卡方近似不准时使用。
*   **Fisher's Exact Test (费雪精确检验)：**
    *   **原理：** 固定行边缘和列边缘总数。
    *   **分布：** 服从**超几何分布 (Hypergeometric distribution)**。
    *   **计算：** 计算在 $H_0$ 假设下，获得当前表格以及比当前表格“更极端”的所有表格的概率之和作为 P-value。
    *   Slide 48 展示了 MATLAB `fishertest` 的输出，包括 Odds Ratio (优势比) 和置信区间。

---

### 前半段复习总结
1.  **选模型不能只看似然值**，否则会过拟合。
2.  **贝叶斯因子 ($K$)** 通过积分掉参数来比较模型，天然带有惩罚复杂度的功能。
3.  **BIC** 是贝叶斯因子的实用替代品，**BIC 越小模型越好**。
4.  **列联表分析**中，大样本用卡方/LRT，小样本用 Fisher 精确检验。

## 2
这份课件的后半部分（Slide 25-40）从二维列联表的贝叶斯分析延伸到了更复杂的**三维列联表（Three-way tables）**，并重点讨论了**辛普森悖论（Simpson's Paradox）**以及三维列联表的**对数线性模型建模**。

以下是后半部分的复习要点梳理：

### 1. 二维列联表的贝叶斯方法 (Slide 25-26)
*   **思路：** 不再仅仅依赖 P 值拒绝原假设，而是直接估计参数的分布。
*   **先验与后验：**
    *   对于多项分布（Multinomial data），其共轭先验是 **Dirichlet 分布**。
    *   如果使用无信息的平坦先验（Flat prior, $\alpha=(1,1,1,1)$），后验分布也是 Dirichlet 分布。
*   **推断方法：**
    *   从后验分布中大量采样。
    *   计算感兴趣的统计量（如风险差 $\delta$、相对风险 $\rho$、优势比 $\theta$）。
    *   通过直方图（Slide 26）展示这些统计量的分布，并计算 **95% 可信区间 (Credible Intervals)**。如果不包含 0（对于差值）或 1（对于比值），则认为有显著差异。

### 2. 三维列联表与辛普森悖论 (Slide 26-32)
*   **结构：** 引入第三个变量（通常是分层变量/混杂因素）。
    *   例子：性别 (A) $\times$ 治疗 (B) $\times$ 康复情况 (C)。
*   **核心考点：辛普森悖论 (Simpson's Paradox)** (Slide 31-32, 62-63)
    *   **现象：** 总体（边缘）分析得出的结论与分层（条件）分析得出的结论完全相反。
    *   **数据解读：**
        *   **边缘优势比 (Marginal Odds Ratio, $\hat{\theta}_{BC}$)**：忽略性别，只看药和康复。结果 $1.48 > 1$，说明**吃药有效**。
        *   **条件优势比 (Conditional Odds Ratio, $\hat{\theta}_{BC|A}$)**：分别看男性和女性。
            *   男性中 OR = 0.553 < 1（吃药更糟）。
            *   女性中 OR = 0.814 < 1（吃药更糟）。
        *   **悖论：** 不管你是男是女，吃药都更糟，但把男女合起来算，吃药却变好了？
    *   **原因：** 存在混杂因素（性别）。在这个数据中，可能康复率本身就与性别强相关，且药物分配在男女中不平衡。
    *   **结论：** 不能只看边缘分布，必须检查分层后的条件分布。

### 3. 三维列联表的建模 (Modeling) (Slide 33-40)
这部分介绍了一系列从简单到复杂的模型，用来描述三个变量 (A, B, C) 之间的独立性关系。这通常对应于**对数线性模型 (Log-Linear Models)**。

#### 模型层级（由简入繁）：

1.  **完全独立模型 (Complete Independence) (Slide 34/67)**
    *   假设：$A, B, C$ 互不相关。
    *   公式：$\pi_{ijk} = \pi_i \pi_j \pi_k$。
    *   自由度最高，拟合最差（$dim=3$）。

2.  **联合独立模型 (Joint Independence) (Slide 34/68)**
    *   假设：两个变量的组合（如 AB）与第三个变量（C）独立。
    *   符号：$(AB, C)$。
    *   这意味着 A 和 B 可能有关联，但它们作为一个整体与 C 无关。

3.  **条件独立模型 (Conditional Independence) (Slide 35/69)**
    *   假设：给定 B 的情况下，A 和 C 独立。
    *   符号：$(AB, BC)$。意味着 A 和 C 之间没有直接连线，它们通过 B 发生联系。
    *   **重要检验：Cochran-Mantel-Haenszel (CMH) Test (Slide 35/70)**
        *   **用途：** 专门用于检验条件独立性。即检验在每一层中，优势比是否都等于 1 ($H_0: \theta_{AC|1} = \theta_{AC|2} = \dots = 1$)。
        *   它是 $2 \times 2 \times K$ 表格分析的黄金标准统计量。

4.  **齐次关联模型 (Homogeneous Association) (Slide 36-37/71-73)**
    *   假设：任意两个变量之间的关联（优势比）在第三个变量的所有层级中是**恒定**的（但不一定为 1）。
    *   特点：没有三阶交互作用 (No three-way interaction)。
    *   符号：$(AB, BC, AC)$。
    *   **重要检验：Breslow-Day Test (Slide 37/73)**
        *   **用途：** 检验优势比的**齐次性 (Homogeneity)**。
        *   $H_0$: 各层的优势比相等 ($\theta_{BC|1} = \theta_{BC|2} = \dots$)。如果 P 值显著，说明存在三阶交互作用（即不同层级的关联程度不同，不能合并）。

5.  **饱和模型 (Saturated Model) (Slide 38-40/79)**
    *   假设：包含所有主效应、二阶交互和三阶交互。
    *   符号：$(ABC)$。
    *   特点：参数最多（$dim=7$），似然值最大，能够完美复现观测数据，但可能过拟合。

### 总结后半段核心：
1.  **警惕辛普森悖论：** 在分析多维列联表时，一定要对比 Marginal 和 Conditional 的结果，防止被聚合数据的假象误导。
2.  **理解模型层级：** 从“全独立”到“饱和模型”，变量间的约束越来越少，拟合越来越好，但参数也越来越多。
3.  **掌握关键检验：**
    *   **Fisher Exact Test:** 小样本独立性。
    *   **CMH Test:** 检验条件独立性（分层后是否有特定关联）。
    *   **Breslow-Day Test:** 检验关联是否在各层间一致（能否合并 OR）。

# 列联表
## 二维
基于课件（特别是第28节课 Lec 28）的内容，我为你详细梳理二维列联表（Two-way Contingency Table）的完整计算流程。

在生物统计中，二维列联表通常用于检验两个分类变量（如：**治疗组别**与**康复情况**）之间是否相互独立。

---

### 第一步：整理原始数据（观测值 $O_{ij}$）
首先，将收集到的频数填入表格。以课件 Slide 42 的数据为例：

| 组别 (Y) \ 结果 (Z) | Covid ($z_1$) | No Covid ($z_2$) | 行合计 ($n_{i+}$) |
| :--- | :--- | :--- | :--- |
| **Vc组** ($y_1$) | 17 ($n_{11}$) | 122 ($n_{12}$) | **139** |
| **Placebo组** ($y_2$) | 31 ($n_{21}$) | 109 ($n_{22}$) | **140** |
| **列合计** ($n_{+j}$) | **48** | **231** | **279** ($n_{++}$) |

*   **观测值 ($O_{ij}$)**：表中的 17, 122, 31, 109。
*   **边缘总计**：行合计（139, 140）和列合计（48, 231）。
*   **样本总数 ($n_{++}$)**：279。

---

### 第二步：计算独立假设下的期望值 ($E_{ij}$)
**核心逻辑**：如果两个变量相互独立，那么某个格子出现的概率应该等于“行概率 $\times$ 列概率”。
**计算公式**：
$$E_{ij} = \frac{\text{该行合计} \times \text{该列合计}}{\text{总样本数}}$$

**课件实例计算（Slide 42）**：
*   $E_{11} (\text{Vc组且Covid}) = \frac{139 \times 48}{279} \approx 23.91$
*   $E_{12} (\text{Vc组且No Covid}) = \frac{139 \times 231}{279} \approx 115.09$
*   $E_{21} (\text{Placebo组且Covid}) = \frac{140 \times 48}{279} \approx 24.09$
*   $E_{22} (\text{Placebo组且No Covid}) = \frac{140 \times 231}{279} \approx 115.91$

---

### 第三步：进行统计检验
我们需要衡量“观测值”和“期望值”之间的差距。差距越大，越不独立。

#### 方法 A：皮尔逊卡方检验 (Pearson’s $\chi^2$)
$$X^2 = \sum_{i,j} \frac{(O_{ij} - E_{ij})^2}{E_{ij}}$$
*   **操作**：对表中的 4 个格子分别计算 $(O-E)^2/E$ 然后求和。

#### 方法 B：似然比检验 (LRT / $G^2$)
$$G^2 = 2 \sum_{i,j} O_{ij} \ln\left(\frac{O_{ij}}{E_{ij}}\right)$$
*   **结果（Slide 42）**：计算得出 $G^2 = 4.87$。

---

### 第四步：确定自由度与 P 值
1.  **自由度 (df)**：对于 $I$ 行 $J$ 列的表，公式为 $df = (I-1) \times (J-1)$。
    *   本例中：$(2-1) \times (2-1) = 1$。
2.  **查找 P 值**：使用卡方分布表或软件（如 `1 - chi2cdf(4.87, 1)`）。
    *   **本例结果**：$P = 0.027$。
3.  **结论**：如果 $P < 0.05$，则**拒绝独立性假设**，认为组别与结果之间存在显著关联（即 Vc 可能影响了 Covid 感染率）。

---

### 第五步：衡量关联强度（优势比 Odds Ratio）
在医学研究中，仅仅知道“有关联”是不够的，还需要知道“多强”。

*   **计算公式**（针对 $2\times2$ 表）：
    $$\theta = \frac{n_{11} \times n_{22}}{n_{12} \times n_{21}}$$
*   **本例计算**：$\theta = \frac{17 \times 109}{122 \times 31} \approx 0.49$
*   **解释**：由于 $\theta < 1$，说明第一组（Vc）相对于第二组（Placebo）发生事件（Covid）的几率更低。

---

### 特殊情况处理（讲义补充点）
1.  **小样本情况**：如果某个格子的期望值 $E_{ij} < 5$，卡方检验就不准了。此时应改用 **Fisher 精确检验 (Fisher's Exact Test)**（Slide 44-48）。
    *   Fisher 检验基于**超几何分布**，计算所有可能出现的表格中，比当前观察到的表格更极端的概率。
2.  **三维干预**：如果存在第三个变量（如性别），需要警惕**辛普森悖论 (Simpson's Paradox)**，此时应使用 **CMH 检验** 进行分层分析（Slide 35/70）。
3.  **贝叶斯视角**：通过 Dirichlet 分布作为先验，利用采样得到参数的置信区间，而不仅仅是一个 P 值（Slide 49-51）。

**复习技巧**：
*   **背公式**：$E = (\text{Row} \times \text{Col}) / \text{Total}$。
*   **记流程**：观测 $\to$ 算期望 $\to$ 算卡方/LRT $\to$ 查 P 值。
*   **分清独立性与关联性**：$H_0$ 永远是“两者没关系”。

## 三维
三维列联表（Three-way Contingency Table）是列联表分析的进阶内容，主要用于处理**三个分类变量**（例如：性别 A、治疗 B、康复情况 C）之间的复杂关系。

根据讲义（特别是 Lec 28 的 Slide 52-80），三维列联表的复习可以分为以下四个核心板块：

---

### 1. 基础结构：边缘与条件 (Marginal vs. Conditional)
三维表可以看作是多个二维表的堆叠。理解它的关键在于如何“切片”：

*   **联合分布 ($n_{ijk}$)**：三个变量同时取某个值的频数。
*   **边缘分布 (Marginal)**：忽略其中一个变量，将数据合并。例如“忽略性别”，看治疗和康复的关系。
    *   **边缘优势比 ($\theta_{BC}$)**：在合并所有层级后计算的 OR 值。
*   **条件分布 / 部分表 (Conditional / Partial Tables)**：固定第三个变量的某一层。例如“只看男性”或“只看女性”。
    *   **条件优势比 ($\theta_{BC|A}$)**：在特定层级（如 A=男性）内计算的 OR 值。

---

### 2. 核心陷阱：辛普森悖论 (Simpson's Paradox)
这是讲义中的重点案例（Slide 61-65）。

*   **定义**：在每一个分层（条件表）中看到的关联方向，在合并数据（边缘表）后竟然**反转**了。
*   **讲义案例解读**：
    *   **条件看**：在男性中，药物 OR < 1（药不如安慰剂）；在女性中，药物 OR < 1（药也不如安慰剂）。
    *   **边缘看**：合并男女数据后，药物 OR > 1（药反而看起来更好了）。
*   **结论**：这是因为存在**混杂因素（Confounding Variable）**。在这个例子中，性别既影响了被分配到哪种治疗的概率，也影响了康复的概率。
*   **复习要点**：在做统计推断时，不能简单合并数据，必须检查变量间是否存在交互作用。

---

### 3. 五种独立性模型 (Modeling Hierarchy)
讲义（Slide 66-80）按照从简单到复杂的逻辑，介绍了描述 A、B、C 三者关系的对数线性模型：

1.  **完全独立模型 $(A, B, C)$**：三个变量各玩各的，互不相关。$\pi_{ijk} = \pi_{i++} \pi_{+j+} \pi_{++k}$。
2.  **联合独立模型 $(AB, C)$**：变量 A 和 B 作为一个整体，与 C 独立。
3.  **条件独立模型 $(AC, BC)$**：**（最常考）** 给定 B 的情况下，A 和 C 独立。
    *   这意味着 A 和 C 之间的关联完全是由 B 中转的。
4.  **齐次关联模型 $(AB, BC, AC)$**：任意两个变量之间的关联（OR 值）在第三个变量的所有层级中都是**相等且恒定**的。
    *   例如：药物对康复的效果在男性和女性中是一样强的（虽然不一定为1）。
5.  **饱和模型 $(ABC)$**：包含所有可能的关联和三阶交互作用，完美拟合观测数据。

---

### 4. 关键统计检验 (Key Tests)
在处理三维表时，你会用到两个最重要的专项检验：

#### A. CMH 检验 (Cochran-Mantel-Haenszel Test) —— Slide 70
*   **用途**：检验**条件独立性**。
*   **核心思想**：它会计算各层（如男、女）的平均优势比。
*   **原假设 $H_0$**：在所有分层中，变量 B 和 C 都是独立的（即合并后的平均 $\text{OR} = 1$）。
*   **适用场景**：当你认为各层的效应方向一致，想要通过合并样本量来增加统计功效时。

#### B. Breslow-Day 检验 —— Slide 73
*   **用途**：检验**齐次性（Homogeneity）**。
*   **原假设 $H_0$**：所有分层的优势比都相等 ($\theta_1 = \theta_2 = \dots = \theta_k$)。
*   **决策意义**：
    *   如果 P > 0.05（不显著）：说明各层效应一致，可以放心使用 CMH 检验给出一个综合的 OR 值。
    *   如果 P < 0.05（显著）：说明存在**三阶交互作用**，不同人群中药物效果不同，此时绝对不能合并数据，必须分层报告结果。

---

### 复习建议：
1.  **分清 OR**：明确什么是局部（Conditional）OR，什么是总体（Marginal）OR。
2.  **理解模型符号**：看到 $(AC, BC)$ 就要反射性想到“给定 C，A 和 B 独立”。
3.  **逻辑顺序**：分析三维表时，通常先做 **Breslow-Day 检验**（看各层稳不稳定），如果稳定，再做 **CMH 检验**（看总体效果显不显著）。

## 三维实现
这五种模型构成了对数线性模型（Log-linear models）的层级体系。它们的实现流程本质上是一个**“简化假设 $\to$ 计算期望值 $\to$ 统计检验 $\to$ 模型选择”**的过程。

我们可以按照课件（Slide 66-80）的逻辑，分步骤讲解如何实现这五种模型。

---

### 实现流程的总逻辑
1.  **明确变量**：设变量为 $A$（如性别）、$B$（如治疗）、$C$（如康复）。观测频数为 $n_{ijk}$，总样本量为 $n$。
2.  **设定模型假设**：选择五种模型中的一种，定义变量间的独立性。
3.  **计算期望频数 ($E_{ijk}$)**：这是实现的关键。根据假设，通过边缘总计（Marginal totals）计算出如果假设成立，每个格子应该有多少人。
4.  **拟合优度检验**：计算似然比统计量 $G^2$（或 $X^2$）和自由度 $df$。
5.  **比较选择**：使用 **BIC** 或 **P值** 在“模型简洁性”和“拟合精确度”之间找平衡。

---

### 具体五种模型的实现细节

#### 1. 完全独立模型 (Complete Independence)
*   **符号表示**：$(A, B, C)$
*   **核心假设**：三个变量之间没有任何关联。
*   **期望值计算 ($E_{ijk}$)**：
    $$E_{ijk} = \frac{n_{i++} \cdot n_{+j+} \cdot n_{++k}}{n^2}$$
    *(注：分子是三个变量的边缘总计连乘，分母是总人数的平方)*
*   **自由度 ($df$)**：$IJK - (I+J+K) + 2$。

#### 2. 联合独立模型 (Joint Independence)
*   **符号表示**：$(AB, C)$
*   **核心假设**：变量 $A$ 和 $B$ 之间可以有关联，但它们作为一个整体与 $C$ 相互独立。
*   **期望值计算 ($E_{ijk}$)**：
    $$E_{ijk} = \frac{n_{ij+} \cdot n_{++k}}{n}$$
    *(注：这里保留了 $AB$ 的联合频数 $n_{ij+}$，所以允许 $AB$ 相关)*
*   **自由度 ($df$)**：$(IJ-1)(K-1)$。

#### 3. 条件独立模型 (Conditional Independence)
*   **符号表示**：$(AC, BC)$ —— **最常用，常用于处理混杂因素**。
*   **核心假设**：在给定变量 $C$ 的情况下，$A$ 和 $B$ 相互独立（即 $A$ 和 $B$ 没直接关系，全靠 $C$ 牵线）。
*   **期望值计算 ($E_{ijk}$)**：
    $$E_{ijk} = \frac{n_{i+k} \cdot n_{+jk}}{n_{++k}}$$
    *(注：这相当于在 $C$ 的每一个层级 $k$ 内，分别做二维表的独立性期望计算)*
*   **自由度 ($df$)**：$K(I-1)(J-1)$。

#### 4. 齐次关联模型 (Homogeneous Association)
*   **符号表示**：$(AB, BC, AC)$
*   **核心假设**：任意两个变量之间都可能有关联，且这种关联（优势比 OR）在第三个变量的所有层级下都是相等的。**没有三阶交互作用**。
*   **实现难点**：此模型**没有**简单的闭式代数公式来直接计算 $E_{ijk}$。
*   **实现方法**：通常使用**迭代比例拟合法 (IPF)** 或软件（如 MATLAB 的 `mvg` 相关函数）进行数值求解。
*   **检验方法**：常用 **Breslow-Day 检验** 来验证这个“齐次性”假设是否成立。

#### 5. 饱和模型 (Saturated Model)
*   **符号表示**：$(ABC)$
*   **核心假设**：包含所有可能的关系（主效应、二阶交互、三阶交互）。
*   **期望值计算 ($E_{ijk}$)**：
    $$E_{ijk} = n_{ijk}$$
*   **结果**：期望值完全等于观测值。拟合是完美的（$G^2 = 0$），但没有任何简化意义。

---

### 如何进行模型选择 (Model Comparison)

课件 Slide 80 给出了一个非常清晰的“Simplicity vs. $\chi^2$ (Fit)”图表：

1.  **从简单开始**：先试 $(A, B, C)$。如果 $G^2$ 很大（P值很小），说明模型太简单，不符合实际。
2.  **逐步增加复杂度**：
    *   如果怀疑有混杂因素，尝试 **条件独立模型** $(AC, BC)$。
    *   如果你发现各层 OR 值不一样，说明需要增加三阶交互作用。
3.  **BIC 决策法（最常用）**：
    *   计算每个模型的 $BIC = G^2 - df \cdot \ln(n)$。
    *   **选择 BIC 值最小的模型**。它能帮你自动剔除那些“虽然增加了拟合度，但浪费了太多参数”的冗余模型。

### 总结
*   **实现流程**：本质上是根据对独立性的不同定义（即分子的组合方式），去分配总样本量 $n$，得到期望值 $E$。
*   **考试技巧**：
    *   看到“给定 $X$ 独立”，找 $(XY, XZ)$ 形式的模型。
    *   看到“$A, B$ 联合独立于 $C$”，找 $(AB, C)$ 形式的模型。
    *   如果 P 值在某个模型显著改善（且 BIC 最小），那个模型就是数据的最佳描述。

# 29
## 1
这份讲义的前半部分（约第1至21张幻灯片，即Screenshot 1-11）主要介绍了**线性模型（Linear Models）**的基础理论、参数估计、模型诊断以及进阶的正则化方法。

以下是详细的内容复习整理：

### 1. 简单线性回归 (Simple Linear Regression)
这部分奠定了线性分析的基础，公式表达为：$y_i = b_0 + b_1 x_i + \epsilon_i$。

*   **核心假设 (Assumptions)**：
    1.  **线性 (Linearity)**：$E[y_i]$ 是 $x_i$ 的线性函数。
    2.  **独立性 (Independence)**：误差项 $\epsilon_i$ 是相互独立的随机变量。
    3.  **正态性 (Normality)**：在每个 $x_i$ 处，误差项均服从正态分布。
    4.  **方差齐性 (Equal Variances/Homoscedasticity)**：所有误差项具有相同的方差 $\sigma^2$。
*   **参数估计：普通最小二乘法 (OLS)**：
    *   目标是最小化残差平方和（RSS）。
    *   推导出了斜率 $\hat{b}_1$ 和截距 $\hat{b}_0$ 的计算公式。
*   **模型评估指标**：
    *   **平方和分解**：$SS_{total} = SS_{regress} + SS_{error}$。
    *   **判定系数 $R^2$**：衡量模型解释数据变异的比例，$R^2 = SS_{regress} / SS_{total}$。
    *   **均方误差 (MSE)**：用于估计误差方差 $\hat{\sigma}^2 = MSE = \frac{SS_{error}}{n-2}$。

### 2. 模型诊断与残差分析 (Residual Analysis)
通过分析残差（Residuals）来验证模型假设是否成立。

*   **残差检查**：检查残差是否偏离均值0，以及方差是否相等。
*   **正态性检验**：
    *   使用 **QQ图 (Quantile-Quantile Plot)** 直观判断。
    *   使用 **Anderson-Darling 检验 (adtest)** 进行定量统计判断（若 $p > 0.05$ 则接受正态性假设）。

### 3. 斜率的显著性检验 (Evaluating Slope)
为了判断自变量 $x$ 是否真的对 $y$ 有影响（即检验 $H_0: b_1 = 0$），讲义介绍了三种方法：

*   **似然比检验 (LRT)**：比较全模型与缩减模型的似然值。在大样本下，$-2\log\lambda$ 服从卡方分布。
*   **t-检验 (t-test)**：通过斜率估计值与其标准误的比值进行检验，这是最常用的方法。
*   **贝叶斯因子 (Bayes Factor)**：通过比较两个模型的边缘似然（Marginal Likelihood）来评价证据强度。

### 4. 多元线性回归 (Multivariable Linear Regression)
当存在多个预测变量时，模型扩展为多元形式。

*   **矩阵表达形式**：$y = X\beta + \epsilon$。其中 $X$ 是设计矩阵（包含常数项列）。
*   **矩阵 OLS 解**：通过微积分推导出正规方程，得到 $\hat{\beta}_{OLS} = (X^T X)^{-1} X^T Y$。

### 5. 共线性与正则化 (Collinearity & Regularization)
*   **共线性 (Collinearity)**：当自变量之间高度相关时，$(X^T X)$ 接近奇异矩阵，导致参数估计极不稳定。
*   **收缩方法 (Shrinkage Methods)**：
    *   **岭回归 (Ridge Regression)**：引入 $L_2$ 范数惩罚项（高斯先验），通过牺牲部分偏差来换取方差的显著降低，解决共线性问题。
    *   **Lasso 回归**：引入 $L_1$ 范数惩罚项（拉普拉斯先验），具有**变量筛选**功能，能将不重要的参数压缩为 0。

---
**复习建议：**
重点掌握线性回归的**四大假设**、**$R^2$ 的含义**以及如何通过 **Matrix OLS 公式**求解参数。对于正则化部分，理解 **Ridge 和 Lasso 在惩罚项上的区别**及其对参数估计的影响是高频考点。


## 2
讲义的后半部分（从第22张幻灯片到结束，即Screenshot 11-21）主要讨论了线性模型的实际应用扩展，包括**分类变量的处理**、**方差分析（ANOVA）的回归视角**以及**广义线性模型（以逻辑回归为例）**。

以下是后半部分的复习要点梳理：

### 1. 多元线性模型的精细化与共线性 (Collinearity)
在处理多个自变量时，讲义通过对比展示了共线性的负面影响：
*   **共线性表现**：当自变量（如 Brain size 和 Height）高度相关时，模型的标准误（SE）会变得很大，导致原本显著的变量变得不显著（p值变大）。
*   **解决思路**：通过变量筛选或使用之前提到的正则化方法（Ridge/Lasso）。

### 2. 分类自变量与哑变量 (Categorical Variables & Dummies)
如何将非数值数据（如性别、种族）放入线性方程？
*   **哑变量 (Dummy Variables)**：将 $N$ 个类别的定性变量转换为 $N-1$ 个 $0/1$ 变量。
    *   例如：性别（男=1，其他=0），种族（种族2=1，其他=0）。
*   **交互项 (Interactions)**：模型不仅考虑单一变量的影响，还考虑变量间的协同作用（如 `x1:x3` 表示大脑尺寸与性别的交互）。
*   **模型选择**：使用 **BIC (贝叶斯信息准则)** 来衡量增加变量带来的模型改善与复杂度之间的平衡。

### 3. 方差分析的回归视角 (ANOVA as a Linear Model)
讲义揭示了 **ANOVA（方差分析）本质上就是一种特殊的线性回归**。
*   **单因素方差分析 (One-way ANOVA)**：比较多组均值是否有差异。
*   **数学表达**：$y_{i,j} = \mu + \tau_j + \epsilon_{i,j}$（总均值 + 组效应 + 随机误差）。
*   **设计矩阵**：在回归模型中，通过设置“参考组（Reference group）”，将其他组与之对比，从而实现方差分析的功能。

### 4. 广义线性模型 (Generalized Linear Model, GLM)
当因变量 $Y$ 不再服从正态分布（例如 $Y$ 是“是/否”的二元结果）时，普通线性回归失效。
*   **GLM 的三个组成部分**：
    1.  **随机成分**：因变量 $Y$ 的分布（如二项分布）。
    2.  **系统成分**：线性预测变量 $\eta = X\beta$。
    3.  **连接函数 (Link Function)**：将线性预测值与均值联系起来的函数 $g(\mu) = \eta$。

### 5. 逻辑回归 (Logistic Regression) —— **重点**
这是处理二分类问题（如：诊断患病 vs 健康）的标准模型。
*   **逻辑函数 (Logit Link)**：$\text{logit}(\pi_i) = \log\left(\frac{\pi_i}{1-\pi_i}\right) = X\beta$。
    *   $\pi_i$ 是事件发生的概率。
    *   $\frac{\pi_i}{1-\pi_i}$ 称为 **Odds (优势比)**。
*   **概率预测公式**：$\pi_i = \frac{e^{X\beta}}{1 + e^{X\beta}}$。该函数将任何实数映射到 $(0,1)$ 区间。
*   **参数估计**：逻辑回归使用 **最大似然估计 (MLE)** 而非 OLS。通过累乘伯努利分布的概率来构建似然函数，寻找使观测数据出现概率最大的 $\beta$ 值。

---
**复习小贴士：**
1.  **对比理解**：一定要明白为什么 ANOVA 可以写成线性回归（通过哑变量实现）。
2.  **公式记忆**：熟记 Logit 函数和概率预测公式，这是理解逻辑回归的关键。
3.  **模型解释**：在线性回归中，$\beta$ 是斜率；在逻辑回归中，$e^{\beta}$ 代表自变量增加一个单位时，结果 **Odds (优势)** 变化的倍数。