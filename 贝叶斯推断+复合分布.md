# 贝叶斯推断
在生物医学统计的考试和作业中，**贝叶斯推断（Bayesian Inference）**的核心逻辑只有一句话：**利用新观察到的数据，来更新你对某个事物的已有认知。**

无论是孟德尔的分离比，还是估算某人的年龄，解题流程都严格遵循以下**五个标准步骤**：

---

### 第一步：确定参数与建立先验分布 (Define Prior)

这是贝叶斯的起点，代表你在看到新数据**之前**的知识状态。

1.  **确定参数 ($\theta$)：** 你想估计的是什么？（例如：分离比 $p$、平均年龄 $\mu$）。
2.  **选择分布模型：**
    *   **如果是概率/比例：** 通常选 **Beta 分布**。
    *   **如果是连续数值（如身高、年龄）：** 通常选 **正态分布 (Normal)**。
3.  **确定先验参数：** 
    *   利用历史数据（如 `hw03_data.txt`）或经验。
    *   *例如：* 孟德尔预期比是 0.75，我们可以设先验为 $Beta(75, 25)$。

---

### 第二步：构建似然函数 (Likelihood)

这代表了**“如果参数是真的，观察到当前数据的概率是多少”**。你需要根据数据的产生方式选择模型：

1.  **二项分布 (Binomial)：** 适用于“对或错”、“显性或隐性”的计数数据（如作业 1 的分离比）。
2.  **正态分布 (Normal)：** 适用于测量数据（如作业 2 的年龄误差、身高测量）。
3.  **泊松分布 (Poisson)：** 适用于单位时间/空间内的发生次数。

---

### 第三步：计算后验分布 (Calculate Posterior)

这是解题的核心，将先验和似然结合。公式为：
$$\text{Posterior} \propto \text{Likelihood} \times \text{Prior}$$

考试中通常有两种解法：

#### 方法 A：共轭先验法（Analytical - 手算常用）
如果先验和似然是“共轭”的，你不需要做复杂的积分，直接套公式更新参数：
*   **场景 1：Beta 先验 + 二项似然 $\rightarrow$ 后验还是 Beta**
    *   先验 $Beta(\alpha, \beta)$，数据观察到 $z$ 个成功，$n-z$ 个失败。
    *   后验参数：$\alpha' = \alpha + z$, $\beta' = \beta + (n - z)$。
*   **场景 2：正态先验 + 正态似然 $\rightarrow$ 后验还是 正态**
    *   后验均值会落在先验均值和样本均值之间（加权平均）。

#### 方法 B：数值计算法（Computational - 复杂情况）
如果分布不共轭（比如先验很奇怪），则需要：
1.  **网格计算 (Grid Approximation)：** 把参数空间切成很多小份，一份份算。
2.  **MCMC 采样：** 像我们之前讲的，通过随机走路模拟出后验分布。

---

### 第四步：后验总结 (Posterior Summary)

算出分布后，你需要给出一个具体的结论。常用的指标有：

1.  **点估计：**
    *   **后验均值 (Mean)**
    *   **MAP (最大后验估计)：** 概率密度最高点，类似频率学派的 MLE。
2.  **区间估计：**
    *   **置信区间 (Credible Interval)：** 比如 95% 置信区间，表示参数有 95% 的概率落在这个范围内。（注意：这与频率学派的 Confidence Interval 定义完全不同）。

---

### 第五步：可视化与结论 (Visualization)

在作业或报告中，通常需要画出一张图，包含三条曲线：
1.  **Prior (虚线)：** 之前的认知。
2.  **Likelihood (点线)：** 数据的证据。
3.  **Posterior (实线)：** 结合后的新认知。

---

### 举例讲解（以作业 2：某人年龄为例）

**1. 题目背景：** 
女儿 18 岁。父亲首次生育时间 $X \sim N(28, 3^2)$。有一份调查数据作为先验。

**2. 解题流程：**
*   **Step 1 (Prior):** 读取文本数据，计算均值和方差。假设先验 $P(\text{Age}) \sim N(46.5, 4^2)$（假设数据拟合结果）。
*   **Step 2 (Likelihood):** “女儿 18 岁”意味着如果父亲现在 $A$ 岁，他生女儿时是 $A-18$ 岁。根据已知分布，似然 $P(\text{Data} | \text{Age}) \sim N(A-18=28, 3^2)$，即 $N(46, 3^2)$。
*   **Step 3 (Posterior):** 两个正态分布相乘。
    *   因为是共轭的，后验均值 $\mu_{post}$ 会更靠近方差更小（信息更准）的那一边。
*   **Step 4 (Conclusion):** 给出更新后的父亲年龄估计值及 95% 概率区间。

---

### 考试技巧：
1.  **分清 Prior 和 Likelihood：** 题目中给出的“历史数据”、“背景常识”是 Prior；“新做的实验”、“新发现的事实”是 Likelihood。
2.  **手算 vs 代码：** 
    *   如果题目给的是简单的 $\alpha, \beta$ 或 $\mu, \sigma$，直接用**共轭公式**手算。
    *   如果题目给的是 `txt` 数据文件，必须先用 Python 拟合参数，或者直接跑 MCMC。
3.  **专业措辞：** 贝叶斯不承认参数是固定的常数，要说“参数服从某种分布”。

掌握了这个流程，你就能应对大多数贝叶斯统计的题目。需要针对作业中具体的 Python 代码实现吗？


# 复合分布
在生物医学统计、精算学或概率论考试中，**复合分布（Compound Distribution）** 是一个高阶考点。它描述的是这样一个随机变量 $S$：

$$S = \sum_{i=1}^{N} X_i$$

其中：
1.  **$N$ 是一个随机变量**（表示事件发生的**次数/频率**，如：一天内病人的就诊次数）。
2.  **$X_i$ 是独立同分布（i.i.d.）的随机变量**（表示每次事件的**强度/损失**，如：每个病人的医疗花费）。
3.  $N$ 与 $X_i$ 相互独立。

以下是处理复合分布问题的标准解题流程：

---

### 第一步：识别组件 (Identify N and X)

解题的第一步是搞清楚 $N$ 和 $X$ 分别服从什么分布。

*   **$N$ (Frequency):** 通常是离散分布，如 **Poisson**、**二项分布** 或 **负二项分布**。
*   **$X$ (Severity):** 可以是离散的，也可以是连续的，如 **指数分布**、**Gamma 分布** 或 **正态分布**。

---

### 第二步：计算期望与方差 (Moments Calculation)

这是考试中最常见的题型。你不需要推导复杂的卷积，只需记住**全期望公式**和**全方差公式**（也叫 Wald 恒等式）：

1.  **期望 $E[S]$：**
    $$E[S] = E[N] \cdot E[X]$$
    *口诀：总平均 = 平均次数 $\times$ 每次的平均强度。*

2.  **方差 $Var(S)$：**
    $$Var(S) = E[N]Var(X) + (E[X])^2 Var(N)$$
    *注意：这个公式非常重要，考试必考。它由两部分组成：$X$ 本身的波动和次数 $N$ 带来的波动。*

---

### 第三步：利用生成函数确定分布 (MGF/PGF Approach)

如果题目要求你求 $S$ 的具体分布或矩生成函数（MGF），使用以下合成公式：

*   **矩生成函数 (MGF):**
    $$M_S(t) = M_N(\ln M_X(t))$$
*   **概率生成函数 (PGF - 针对离散 $X$):**
    $$P_S(z) = P_N(P_X(z))$$

**考试常客：复合泊松分布 (Compound Poisson)**
如果 $N \sim \text{Poisson}(\lambda)$，那么：
*   $E[S] = \lambda E[X]$
*   $Var(S) = \lambda E[X^2]$

---

### 第四步：计算特定概率 (Probability Calculation)

如果题目要求计算 $P(S = s)$ 或 $P(S > s)$，通常有三种方法：

1.  **直接卷积法 (Convolution)：**
    适用于 $N$ 的取值范围很小（如 $N$ 只能取 0, 1, 2）。
    $$P(S=s) = \sum_{n=0}^{\infty} P(S=s|N=n)P(N=n)$$
2.  **递归法 (Panjer Recursion)：**
    如果是精算考试，可能会用到 Panjer 递归公式，适用于 $N$ 属于 $(a, b, 0)$ 类分布。
3.  **近似法 (Normal Approximation)：**
    **当 $E[N]$ 很大时**（例如 $\lambda > 30$），根据中心极限定理，复合分布 $S$ 近似服从正态分布：
    $$S \sim N(E[S], Var(S))$$
    然后通过 Z 分数查表解决。

---

### 第五步：Python 模拟流程 (Computational Approach)

如果在上机考试中遇到复合分布，最简单的方法是**蒙特卡洛模拟**。

**场景：** 某医院一天病人就诊次数 $N \sim \text{Poisson}(10)$，每个病人的花费 $X \sim \text{Exp}(500)$。求总花费 $S$ 大于 6000 的概率。

```python
import numpy as np

# 1. 设定模拟次数
n_simulations = 100000
s_results = []

for _ in range(n_simulations):
    # 2. 抽取随机次数 N
    n = np.random.poisson(10)
    
    # 3. 根据 N 抽取 N 个 X 并求和
    if n > 0:
        s = np.random.exponential(500, n).sum()
    else:
        s = 0
    s_results.append(s)

# 4. 计算概率
prob = np.mean(np.array(s_results) > 6000)
print(f"总花费大于6000的概率为: {prob}")
```

---

### 总结答题模版

1.  **列出参数：** 写出 $E[N], Var(N), E[X], Var(X)$。
2.  **套用公式：** 计算 $E[S]$ 和 $Var(S)$。
3.  **判断方法：**
    *   小样本/离散 $\rightarrow$ 条件概率求和。
    *   已知分布组合（如 Poisson-Gamma） $\rightarrow$ 识别为负二项分布。
    *   大样本 $\rightarrow$ 正态近似。
4.  **给出结论：** 结合题目背景说明实际意义（如：保险公司应准备多少准备金）。

**考试避坑点：**
*   **$N=0$ 的情况：** 别忘了当 $N=0$ 时，$S=0$。
*   **单位统一：** 检查 $X$ 的期望和 $N$ 的周期（如“每小时”与“每天”）是否一致。
*   **方差公式：** 很多人会漏掉 $Var(S)$ 公式里的第二项 $(E[X])^2 Var(N)$，一定要背熟。