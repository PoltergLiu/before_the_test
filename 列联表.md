# 二维
在生物医学统计中，**二维列联表（Two-way Contingency Table）**主要用于分析两个分类变量之间的相关性。

针对你试卷中的 **Question 4**，它其实涉及到了更高阶的**三维列联表（$G \times I \times H$）**，但其核心逻辑依然是基于二维表的扩展。

以下我将先讲解最基础的二维表流程，再针对你试卷中提到的**三种独立性模型**进行深度解析。

---

### 一、 基础流程：Pearson 卡方独立性检验
这是最常见的二维表（$R \times C$）解题步骤：

1.  **建立假设：**
    *   $H_0$: 变量 A 与 变量 B 相互独立（无关联）。
    *   $H_1$: 变量 A 与 变量 B 不独立（有关联）。

2.  **计算期望频数 ($E_{ij}$):**
    *   公式：$E = \frac{\text{该行合计} \times \text{该列合计}}{\text{总样本量}}$
    *   **考试注意：** 如果任何一个格子的 $E < 5$，通常需要使用 **Fisher 精确检验**。

3.  **计算卡方统计量 ($\chi^2$):**
    *   公式：$\chi^2 = \sum \frac{(O - E)^2}{E}$ （$O$ 为实际观察值，即题目表中的数字）。

4.  **确定自由度 ($df$):**
    *   $df = (r - 1) \times (c - 1)$ （行数减1乘以列数减1）。

5.  **得出结论：**
    *   查表或看 P 值。若 $P < 0.05$，拒绝 $H_0$，说明两个变量有关联。

---

### 二、 进阶流程：应对试卷 Question 4 的三种模型
你试卷的 Q4 要求拟合三种独立性模型，这是**分类数据分析（Categorical Data Analysis）**的高级考点。这三种模型决定了你如何计算“期望频数”。

#### 1. 完全独立模型 (Complete Independence Model) - 符号：$(G, I, H)$
*   **含义：** 性别(G)、信息意见(I)、健康意见(H) 三者之间没有任何关系。
*   **期望值计算：** $E_{gih} = n \times P(G) \times P(I) \times P(H)$
    *   即：$E = \frac{G_{\text{合计}} \times I_{\text{合计}} \times H_{\text{合计}}}{n^2}$

#### 2. 联合独立模型 (Joint Independence Model) - 符号：$(G, IH)$
*   **含义：** 性别(G) 与 “两个意见的组合(I+H)” 相互独立。即：知道意见并不能帮你预测性别。
*   **期望值计算：** $E_{gih} = \frac{G_{\text{合计}} \times (IH)_{\text{合并合计}}}{n}$

#### 3. 条件独立模型 (Conditional Independence Model) - 符号：$(GI, GH)$
*   **含义：** 在给定性别(G)的情况下，两个意见(I 和 H)是独立的。即：性别是中介因素。
*   **期望值计算：** $E_{gih} = \frac{(GI)_{\text{合计}} \times (GH)_{\text{合计}}}{G_{\text{合计}}}$

---

### 三、 Python 代码实现 (以 Question 4 为例)

在 Python 中，处理这类复杂的列联表模型通常使用 `scipy.stats` 进行简单检验，或使用 `statsmodels` 进行对数线性模型（Log-linear Models）拟合。

```python
import numpy as np
import pandas as pd
from scipy.stats import chi2_contingency

# 1. 整理数据 (根据试卷表格)
# Gender: Male(0), Female(1)
# Info: Support(0), Oppose(1)
# Health: Support(0), Oppose(1)
data = np.array([
    [[76, 160], [6, 25]],   # Male: [I_Supp[H_S, H_O], I_Opp[H_S, H_O]]
    [[114, 181], [11, 48]]  # Female: ...
])

# 2. 基础二维卡方检验 (假设我们只看 Gender 和 Health Opinion)
# 先把数据压缩成 2x2
gender_health = data.sum(axis=1) # 合并 Information Opinion
chi2, p, dof, expected = chi2_contingency(gender_health)

print(f"卡方值: {chi2}, P值: {p}")
```

**针对 Q4 的模型比较：**
在考试中，你需要对三个模型分别计算 $\chi^2$。
*   **模型选择准则：** 观察 **卡方值** 或 **AIC/BIC**。通常选择卡方值较小（即期望值与观察值最接近）且模型最简单的那个。如果某个模型的 $P > 0.05$，说明该模型对数据的拟合是“可接受的”。

---

### 四、 答题套路总结

1.  **列出观察表 (Observed)：** 抄写题目中的原始数据。
2.  **计算期望表 (Expected)：** 根据题目要求的模型（完全、联合或条件独立），使用对应的公式计算出每个格子的期望值。**这是 Q4 的拿分重灾区（9分）**。
3.  **计算卡方值：** 对每个格子执行 $(O-E)^2/E$ 并求和。
4.  **查表判别：**
    *   自由度计算：
        *   完全独立：$df = n_{\text{cells}} - (r+c+l) + 2$
        *   联合独立：由具体合并方式决定。
5.  **结论：** “根据 Pearson 卡方检验，模型 XX 的 P 值最大（或卡方最小），拟合效果最好，因此我们倾向于选择 XX 模型。”

**温馨提示：** 
上科大的这道题（35分！）非常考察对**期望频数公式**的记忆。建议你在草稿纸上先把 $G, I, H$ 的边际合计（Row/Column/Layer Totals）全部算出来，后面套公式会快很多。




# 三维
在生物医学统计的考试中，**三维列联表（3D Contingency Table）** 的分析是分类数据分析（Categorical Data Analysis）的高级部分。

三维列联表通常涉及三个分类变量，假设为 $X, Y, Z$。在你的试卷中，这三个变量是：**Gender (G)**, **Information Opinion (I)**, 和 **Health Opinion (H)**。

解题流程的核心在于：**根据不同的独立性假设，计算“期望频数 ($E_{ijk}$)”并进行拟合优度检验。**

---

### 第一步：整理边际合计 (Marginal Totals)
在动手计算任何模型前，先算出各个维度的合计，这会大大简化后面的计算：
1.  **一维合计：** $n_{g++}$ (男/女总数), $n_{+i+}$ (支持/反对I总数), $n_{++h}$ (支持/反对H总数)。
2.  **二维合计：** $n_{gi+}$ (性别与I的交叉合计), $n_{g+h}$ (性别与H的交叉合计), $n_{+ih}$ (I与H的交叉合计)。
3.  **总样本量：** $n$。

---

### 第二步：根据模型计算期望频数 ($E_{gih}$)

这是试卷 Question 4 的核心考点（9分）。针对不同的独立性模型，公式如下：

#### 1. 完全独立模型 (Model of Complete Independence)
*   **符号表示：** $(G, I, H)$
*   **假设：** 三个变量之间没有任何关联，像三个独立的硬币。
*   **期望频数公式：**
    $$E_{gih} = \frac{n_{g++} \times n_{+i+} \times n_{++h}}{n^2}$$

#### 2. 联合独立模型 (Model of Joint Independence)
*   **符号表示：** $(G, IH)$ —— 对应试卷中的“(2) Joint independence model (G, IH)”
*   **假设：** 性别 (G) 与“两个意见的组合 (I, H)”是独立的。
*   **期望频数公式：**
    $$E_{gih} = \frac{n_{g++} \times n_{+ih}}{n}$$
    *（注：分子是“性别的合计”乘以“I和H在不分性别时的二维单元格数值”）*

#### 3. 条件独立模型 (Model of Conditional Independence)
*   **符号表示：** $(GI, GH)$ —— 对应试卷中的“(3) Conditional independence model”
*   **假设：** 在**给定性别 (G)** 的前提下，两个意见 I 和 H 是独立的。
*   **期望频数公式：**
    $$E_{gih} = \frac{n_{gi+} \times n_{g+h}}{n_{g++}}$$
    *（注：这是在每个性别层内，按二维独立性公式计算后的结果）*

---

### 第三步：计算检验统计量 (Pearson $\chi^2$)

对于以上每一个模型，都要计算其卡方值以评价拟合好坏：
$$\chi^2 = \sum \frac{(O_{gih} - E_{gih})^2}{E_{gih}}$$
其中 $O$ 是题目给出的原始 8 个单元格数值。

---

### 第四步：确定自由度 ($df$)

这是考试最容易扣分的地方。假设 $G$ 有 $I$ 个水平，$I$ 有 $J$ 个水平，$H$ 有 $K$ 个水平（本题中均为 2）：

1.  **完全独立 $(G, I, H)$：**
    $df = IJK - (I + J + K) + 2 = 8 - (2+2+2) + 2 = 4$
2.  **联合独立 $(G, IH)$：**
    $df = IJK - (1 + (I-1) + (JK-1)) = (I-1)(JK-1) = (2-1)(4-1) = 3$
3.  **条件独立 $(GI, GH)$：**
    $df = I(J-1)(K-1) = 2(2-1)(2-1) = 2$

---

### 第五步：模型比较与优选 (Model Selection)

**考试逻辑：**
1.  **看 P 值：** 如果某个模型的 $P > 0.05$（卡方值小于临界值），说明该模型**接受**。
2.  **选最优：** 
    *   如果有多个模型都接受，通常选择**最简单**的模型（自由度最大的那个）。
    *   或者选择 **卡方值最小** 的模型（拟合误差最小）。
    *   在生物医学研究中，通常 **条件独立模型** 更有意义，因为它能揭示“性别”是否是影响两种意见关联的中介因素。

---

### 第六步：Python 操作流程

在 Python 中，通常使用对数线性模型（Log-linear Model）来处理：

```python
import statsmodels.api as sm
import statsmodels.formula.api as smf
import pandas as pd

# 1. 将 8 个格子的数据转为长格式 DataFrame
# columns: [gender, info, health, count]

# 2. 拟合条件独立模型 (GI, GH)
# 公式中不需要 I*H 项
model_cond = smf.glm("count ~ gender*info + gender*health", 
                      data=df, 
                      family=sm.families.Poisson()).fit()

# 3. 输出期望值
print(model_cond.predict()) 

# 4. 查看偏差 (Deviance)，即 G-square 统计量
print(model_cond.deviance)
```

### 总结答题要点（针对 Question 4）：
*   **Expectation values (9分)：** 必须列出 3 个模型下，每个格子的计算结果。建议画三个 2x2x2 的表。
*   **Pearson chi-square test (9分)：** 每个模型算出一个 $\chi^2$ 总和。
*   **Which model prefer (5分)：** 比较卡方值。通常这类医学社会调查数据，意见之间会有微弱关联，**条件独立模型** 往往拟合得最好。

**提示：** 考试时如果计算器按得慢，先写出公式和代入的数字（如：$E_{111} = \frac{267 \times 190 \times 201}{720^2}$），哪怕最后结果算错，步骤分也能拿大头。



# python流程
在生物医学统计考试中，使用 Python 处理列联表主要分为两个场景：**二维表**主要用于基础的独立性检验，**三维表**则需要通过对数线性模型（Log-linear Models）来处理更复杂的独立性关系。

以下是针对考试流程的 Python 核心代码实现。

---

### 一、 二维列联表 (2D Contingency Table)
**适用场景：** 检验两个分类变量（如：性别 vs 治疗效果）是否相关。
**核心库：** `scipy.stats`

#### 流程与代码：
```python
import numpy as np
import pandas as pd
from scipy.stats import chi2_contingency

# 1. 建立观察值矩阵 (Observed Table)
# 示例：行代表性别(男/女)，列代表意见(支持/反对)
observed = np.array([
    [76, 160],  # 男性
    [114, 181]  # 女性
])

# 2. 执行卡方检验
# 返回值：chi2(卡方值), p(P值), dof(自由度), expected(期望频数)
chi2, p, dof, expected = chi2_contingency(observed)

print(f"卡方统计量: {chi2:.4f}")
print(f"P值: {p:.4f}")
print(f"自由度: {dof}")
print("期望频数表:")
print(expected)

# 3. 判定结论
alpha = 0.05
if p < alpha:
    print("结论：拒绝原假设，两个变量显著相关。")
else:
    print("结论：不拒绝原假设，两个变量相互独立。")
```

---

### 二、 三维列联表 (3D Contingency Table)
**适用场景：** 处理类似试卷 **Question 4** 的情形，涉及三个变量（G, I, H），需要拟合不同的独立性模型。
**核心工具：** `statsmodels`（使用泊松回归模拟对数线性模型）。

#### 1. 数据准备 (长格式)
在处理三维表时，必须将数据转换为“长格式”（Long Format）。

```python
import statsmodels.api as sm
import statsmodels.formula.api as smf

# 根据试卷 Question 4 数据建模
data = {
    'Gender': ['M','M','M','M', 'F','F','F','F'],
    'Info':   ['S','S','O','O', 'S','S','O','O'], # S:Support, O:Oppose
    'Health': ['S','O','S','O', 'S','O','S','O'],
    'Count':  [76, 160, 6, 25, 114, 181, 11, 48]
}
df = pd.DataFrame(data)
```

#### 2. 三种模型的 Python 实现

针对考试要求的不同独立性，通过修改 `formula` 来实现：

##### (1) 完全独立模型 (Complete Independence)
公式：`Count ~ Gender + Info + Health`
```python
# 模型：(G, I, H) 三者互不相关
model_comp = smf.glm("Count ~ Gender + Info + Health", data=df, 
                     family=sm.families.Poisson()).fit()

print("完全独立模型期望值:")
print(model_comp.predict()) # 这就是考试要求的 Expectation values
print(f"卡方/偏差 (Deviance): {model_comp.deviance:.4f}")
```

##### (2) 联合独立模型 (Joint Independence)
公式：`Count ~ Gender + Info * Health`
（意为 Gender 与 "Info和Health的组合" 独立）
```python
# 模型：(G, IH)
model_joint = smf.glm("Count ~ Gender + Info * Health", data=df, 
                      family=sm.families.Poisson()).fit()

print("联合独立模型 Deviance:", model_joint.deviance)
```

##### (3) 条件独立模型 (Conditional Independence)
公式：`Count ~ Gender * Info + Gender * Health`
（意为在给定 Gender 的情况下，Info 和 Health 独立）
```python
# 模型：(GI, GH)
model_cond = smf.glm("Count ~ Gender * Info + Gender * Health", data=df, 
                     family=sm.families.Poisson()).fit()

print("条件独立模型期望值:")
print(model_cond.predict())
print(f"条件独立模型 Deviance: {model_cond.deviance:.4f}")
```

---

### 三、 考试实战技巧：如何评价模型？

在 Python 运行出结果后，你需要在试卷上写下以下对比逻辑：

1.  **期望值对比：** 题目问 `Expectation values`，你就把 `model.predict()` 得到的 8 个数字填入表格。
2.  **拟合优度判定：**
    *   查看每个模型的 **Deviance（偏差）**。在对数线性模型中，Deviance 类似于卡方统计量。
    *   **P值计算：** 考试中通常需要手动或用 Python 计算：
        ```python
        from scipy.stats import chi2
        p_val = 1 - chi2.cdf(model.deviance, model.df_resid)
        ```
    *   **判定结论：** 如果 $P > 0.05$，说明该模型对数据的拟合是**可以接受的**。
3.  **模型优选 (Which model will you prefer?)：**
    *   通常选择 **Deviance 最小** 且 **P值显著大于 0.05** 的模型。
    *   如果多个模型都通过了，选 **自由度 (df_resid) 较大** 的那个（模型更简洁）。

### 总结：
*   **二维表：** 直接用 `chi2_contingency`，一秒出结果。
*   **三维表：** 必须用 `smf.glm`。
    *   加号 `+` 表示独立。
    *   乘号 `*` 表示存在关联（主效应+交互效应）。
    *   **考试口诀：** 题目要求谁和谁独立，公式里就不要让它们相乘。

