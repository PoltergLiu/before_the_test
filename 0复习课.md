# 1
根据你提供的讲义，**第一部分（Slide 3-7）**的主题是**“概率论基础与统计决策（Probability Basics & Expectation）”**。

这部分内容虽然基础，却是整门课程的灵魂，因为它引入了**贝叶斯推理**的核心逻辑，并展示了如何利用**数学期望**在生物医学场景下做最优决策。

以下是详细讲解：

---

### 1. 贝叶斯定理：从“结果”推测“原因” (Slide 3)
*   **全概率公式 (Law of Total Probability)**：如果你想知道事件 $B$ 发生的概率，但 $B$ 取决于多个互斥的状态 $A_1, A_2, \dots$（比如一个人是否患病、性别、年龄段等），你需要把每种状态下 $B$ 发生的概率加权平均。
*   **贝叶斯公式 (Bayes' Theorem)**：
    $$P(A_i|B) = \frac{P(A_i)P(B|A_i)}{P(B)}$$
    *   这是统计学的“时空倒置”。我们通常容易观测到 $P(B|A_i)$（已知患病，检测呈阳性的概率），但实际应用中我们需要知道的是 $P(A_i|B)$（检测呈阳性，我**到底**有没有病）。

### 2. 新冠检测悖论：为什么高精度检测也会“失准”？ (Slide 4)
讲义通过一个经典的生物医学例子展示了**先验概率（Prevalence）**的重要性：
*   **参数设置**：
    *   **灵敏度 (Sensitivity, $P(+|D)$)**：99%（病人能被检出）。
    *   **特异性 (Specificity, $P(-|\bar{D})$)**：95%（健康人能被排除）。
    *   **患病率 (Prevalence, $P(D)$)**：0.1%（这是关键，极低！）。
*   **计算结果**：如果一个人检测呈阳性，他真实患病的概率 $P(D|+)$ **仅为 1.9%**。
*   **核心结论**：当一种疾病非常稀有时（先验概率极低），即使检测手段极其精准，阳性结果中的绝大多数依然是“假阳性”。这解释了为什么大规模筛查时，必须考虑人群的先验背景。

### 3. 期望值与公共卫生优化：混合采样检测 (Slide 5-6)
这部分讨论了如何通过数学手段节省核酸检测资源。
*   **方案 A**：每个人单独测，总次数 = $N$。
*   **方案 B（混采）**：把 $s$ 个人的一份样本混在一起测（共 $m$ 组，$N = ms$）。
    *   如果这组是阴性：只需 1 次检测。
    *   如果这组是阳性：这组里的 $s$ 个人都要重测，总次数 = $1 + s$ 次。
*   **期望检测次数 $E[X]$ 的计算**：
    *   一组人全为阴性的概率：$(1-\pi)^s$（$\pi$ 是阳性率）。
    *   一组人中至少有一个阳性的概率：$1-(1-\pi)^s$。
    *   **单组期望**：$E[X] = 1 \cdot (1-\pi)^s + (s+1)[1-(1-\pi)^s]$。

### 4. 数学推导与最优组大小 (Slide 7)
教授在这里展示了如何利用数学工具（泰勒展开和均值不等式）来寻找最优解。
*   **简化模型**：当阳性率 $\pi$ 很小时，利用近似公式 $(1-\pi)^s \approx 1 - s\pi$。
*   **化简结果**：总检测期望次数大约是 $N(\frac{1}{s} + \pi s)$。
*   **求极值**：利用均值不等式（AM-GM Inequality），当 $\frac{1}{s} = \pi s$ 时，检测次数最少。
*   **结论**：
    *   **最优组大小** $s \approx 1/\sqrt{\pi}$。
    *   **最省钱的检测次数**约为 $2N\sqrt{\pi}$。
    *   **实际意义**：如果阳性率是 1%，那么 10 个人一组混采是最科学的。

---

### 第一部分的复习建议：
1.  **分清三个概率**：不要搞混 **灵敏度**（已知病看检测）、**特异性**（已知健康看检测）和 **后验概率**（已知检测看是否患病）。
2.  **理解先验的影响**：记住，后验概率不仅取决于实验精度，更取决于你对背景情况（先验）的了解。
3.  **公式记忆**：熟练掌握二项分布背景下的期望计算，这经常作为计算题出现。

这部分内容为你接下来的**后验估计（Posterior Inference）**打下了基础：如果你把“先验”换成一个分布（比如 Beta 或 Normal），而不仅仅是一个数字，那就进入了作业三所涉及的高级贝叶斯领域。

---

# 2
讲义的**第二部分（Slide 8-17）**是整门课程的核心，主题是**“贝叶斯后验推断（Posterior Inference）”**。

如果说第一部分是概率论的“开胃菜”，那么这一部分就是真正的“正餐”。它讲解了如何将**先验知识（Prior）**与**观测数据（Likelihood）**结合，通过数学推导得到**后验分布（Posterior）**。

这部分内容直接对应你的**作业三**。我们可以将其分为三个核心模型来理解：

---

### 1. 正态-正态模型 (Normal-Normal Model) —— 连续变量估计 (Slide 8-10)
这部分用“放疗后体重估计”作为案例：
*   **先验 (Prior)**：根据历史经验或之前的调查（Empirical data），我们认为某人的体重服从 $N(49.5, 0.9^2)$。
*   **似然 (Likelihood)**：现在新测得一个数据 $x=47$，且已知测量误差的标准差为 $1$。
*   **后验分布 (Posterior)**：这是两个正态分布的“博弈”。
    *   **均值公式**：后验均值是先验均值和观测值的**加权平均**。权值取决于各自的方差（方差越小，权重越大）。
    *   **结果**：计算得出后验均值 $\hat{\mu} = 48.38$。你会发现这个值介于先验（49.5）和观测值（47）之间，但更偏向观测值，因为观测的精度相对较高。
*   **置信区间 (Credible Interval)**：贝叶斯派不说“置信区间（Confidence Interval）”，而叫“可靠区间（Credible Interval）”。Slide 10 展示了 95% 的 CI 范围。

### 2. Beta-二项模型 (Beta-Binomial Model) —— 比例与概率估计 (Slide 11-14)
这部分解决的是“成功率”或“发生率”的问题，对应你的**作业三第一题（分离比估计）**。
*   **背景**：新冠死亡率或疫苗效力估计。
*   **共轭先验**：如果你观测的数据符合二项分布（如：10个人里3个死亡），那么最方便的先验分布就是 **Beta 分布**。
*   **核心逻辑（简单加法）**：
    *   先验：$Beta(\alpha, \beta)$。
    *   观测：$n$ 次试验，成功 $k$ 次。
    *   后验：$Beta(\alpha + k, \beta + n - k)$。
    *   **Slide 12 案例**：先验是 $Beta(1.64, 20.3)$，新医院数据是 $3/10$，后验直接变为 $Beta(1.64+3, 20.3+7) = Beta(4.64, 27.3)$。
*   **疫苗效力 (VE)**：Slide 13-14 展示了如何用贝叶斯方法计算 VE。VE 是一个复合参数 $\frac{ARU - ARV}{ARU}$，通过对 $ARU$ 和 $ARV$ 分别建立 Beta 后验，再通过蒙特卡洛模拟或数值积分得到 VE 的后验分布。

### 3. Dirichlet-多项模型 (Dirichlet-Multinomial Model) —— 列联表分析 (Slide 15-17)
这是 Beta-二项模型的“高维版本”，用于分析 $2 \times 2$ 或更高维度的表格。
*   **先验**：**Dirichlet 分布**（可以看作是分布的分布）。
*   **参数更新**：同样是简单的加法。如果你观测到一组计数 $n = (n_{11}, n_{12}, n_{21}, n_{22})$，只需将这些数加到先验参数 $\alpha$ 上。
*   **统计量提取**：
    *   **风险差 ($\delta$)**：两组概率之差。
    *   **相对风险 ($\rho$)**：两组概率之比。
    *   **比值比 ($\theta$, Odds Ratio)**：医学研究中最常用的指标。
*   **Slide 17** 展示了这些指标的后验分布图。你会看到，贝叶斯方法不仅给你一个数值，还给你整个分布曲线，让你清晰地看到不确定性。

---

### 第二部分的复习要点（考试高频）：

1.  **共轭性 (Conjugacy)**：一定要记住哪种数据配哪种先验。
    *   连续测量（如身高体重） $\rightarrow$ **正态分布**。
    *   二元分类（如生死、显隐性） $\rightarrow$ **Beta 分布**。
    *   多元分类（如列联表） $\rightarrow$ **Dirichlet 分布**。
2.  **后验均值的移动**：理解为什么后验均值总是出现在先验均值和样本均值之间。如果样本量 $n$ 非常大，后验会完全被数据主导，先验的影响会消失。
3.  **贝叶斯 vs 频率派**：
    *   频率派关注 $p$ 值（是否有显著差异）。
    *   贝叶斯派关注后验概率分布（这件事情发生的可能性到底是多少）。
4.  **计算技巧**：在 Beta-二项模型中，后验参数的更新就是简单的“加法”，这是最容易拿分的计算题。

**联系作业三**：
你的作业三要求你分析孟德尔的数据（0.75的分离比）。你首先要确定一个 Beta 先验（通常可以用 $Beta(3,1)$ 或者根据孟德尔其他实验拟合出一个 $\alpha, \beta$），然后把新的 42 组模拟数据加进去更新参数。这正是 Slide 12 所演示的过程。

这部分内容理解了吗？如果需要，我们可以继续讲解**第三部分（假设检验与ANOVA）**。

# 3

讲义的**第三部分（Slide 18-27）**从贝叶斯统计回到了**经典频率派统计（Frequentist Statistics）**，核心主题是**“假设检验与方差分析（Hypothesis Testing & ANOVA）”**。

这部分内容在生物医学研究中应用最广（比如判断药物是否有效、不同组别是否有显著差异）。我们可以将其分为三个模块来理解：

---

### 1. T 检验：两组均值的比较 (Slide 18-22)

这是统计学中最常用的工具，用来判断两组数据的均值差异是否是由随机误差引起的。

*   **基本原理 (Slide 18)**：
    *   建立 $H_0$（零假设：两组没区别）和 $H_1$（备择假设：有区别）。
    *   **已知方差 $\sigma$**：用 Z 检验。
    *   **未知方差 $\sigma$**：用 T 检验（用样本标准差 $s$ 代替 $\sigma$）。
*   **Student's t-test (Slide 19)**：
    *   **前提条件**：数据服从正态分布，且**两组方差相等**。
    *   **自由度**：$df = n_1 + n_2 - 2$。
*   **Welch's t-test (Slide 20)**：
    *   **应用场景**：当两组方差差异很大（比如 $s_1/s_2 > 2$）时，不能假设方差相等。
    *   **关键点**：使用 **Welch-Satterthwaite 近似公式** 来重新计算一个复杂的自由度 $\vartheta$。
*   **软件实现 (Slide 22)**：讲义展示了 MATLAB/R 的代码（`ttest2`）。
    *   **$h=1$**：表示拒绝零假设，差异显著。
    *   **$p$ 值**：如果 $p < 0.05$（或给定的 $\alpha$），说明结果在统计学上是显著的。

---

### 2. 单因素方差分析 (One-way ANOVA) (Slide 23-24)

当你要比较的组别**超过两组**（例如：安慰剂组、药物A组、药物B组、联合用药组）时，不能做多次 T 检验（因为会增加假阳性率），必须使用 ANOVA。

*   **模型公式 (Slide 23)**：
    $$y_{i,j} = \mu + \tau_j + \epsilon_{i,j}$$
    *   $\mu$：总平均值。
    *   $\tau_j$：第 $j$ 种处理带来的额外效应。
    *   $\epsilon_{i,j}$：随机误差。
*   **严格的前提检查 (Slide 24)**：这是考试常考的细节！
    *   **正态性检查**：KS检验、AD检验或SW检验。
    *   **方差齐性检查（各组波动是否差不多）**：Bartlett检验或Levene检验。只有通过了这些检查，ANOVA 的结果才可信。

---

### 3. ANOVA 的回归视角与哑变量 (Slide 25-27)

这部分是课程进阶的内容：**所有的方差分析本质上都是线性回归**。

*   **矩阵表达 (Slide 25)**：展示了如何把多组数据写成 $y = X\beta + \epsilon$ 的形式。$X$ 被称为**设计矩阵（Design Matrix）**。
*   **哑变量 (Dummy Variables) (Slide 26)**：
    *   为了在回归方程里表示“类别”（如药物类型），我们需要引入 0 和 1。
    *   **基准组 (Reference)**：通常选一组（如 Placebo 安慰剂组）作为 0 基准，其他组的系数表示相对于基准组的增量。
*   **模型比较 (Slide 27)**：
    *   通过 `fitglm` 等函数拟合模型。
    *   **F 检验**：用来判断整个模型（所有药物组加起来）是否显著优于“什么都不做的模型（只有截距项）”。

---

### 第三部分的复习要点：

1.  **什么时候用 Welch's 而不是 Student's T？**
    *   看方差（标准差的平方）。如果两组波动明显不一样，必须用 Welch's，否则 $p$ 值会失效。
2.  **$p$ 值的含义**：
    *   记住：$p < 0.05$ 并不代表药物一定有效，它代表的是“如果药物真的无效，我们观察到目前这么大差异的概率小于 5%”。
3.  **ANOVA 的局限性**：
    *   ANOVA 只能告诉你“这几组里至少有一组不一样”，但它不能直接告诉你“到底是哪两组不一样”。要解决后者，需要进行**事后检验（Post-hoc test）**。
4.  **联系作业四**：
    *   在 RNA-seq 数据分析中，你会遇到很多基因的表达量比较。这时候就要判断：这个基因在四个重复实验里的波动（方差）是否符合某些分布假设（如 Poisson 或正态），这正是 Slide 24 强调的前提检查。

**这部分内容逻辑非常严密：先检查前提（正态、方差齐性），再选择工具（T检验或ANOVA），最后通过 $p$ 值下结论。**

我们要继续讲解**第四部分（模型测试与分布验证）**吗？


# 4
讲义的**第四部分（Slide 28-32）**进入了一个更高级的统计话题：**模型检验与分布拟合（Model Testing & Distribution Validation）**。

如果说第三部分是在比较两组数的“平均值”是否有差异，那么第四部分就是在问：**“我的整堆数据，到底符合哪个数学分布？”** 这一部分与你的**作业四（RNA-seq数据分布分析）**逻辑高度吻合。

以下是详细拆解：

---

### 1. 从二项分布 (Binomial) 开始 (Slide 28)
*   **案例背景**：研究家庭中男孩的数量（$n=12$ 个孩子的家庭）。
*   **基本假设**：假设生男生女概率固定（$\pi \approx 0.5$），且每个孩子的性别独立。这对应**二项分布 $X \sim Bin(n, \pi)$**。
*   **参数估计**：通过最大似然估计（MLE）算出 $\hat{\pi}_{MLE} = 0.5192$。
*   **问题**：虽然算出了概率，但实际数据分布（直方图）与理论模型（折线）真的吻合吗？

### 2. 发现“超离散”现象 (Slide 29-30)
*   **现实矛盾**：观察 Slide 28 的图会发现，实际数据在两头（男孩子特别多或特别少）的频率比理论模型预言的要高。这种实际方差大于理论方差的现象叫**超离散 (Overdispersion)**。
*   **引入 Beta-Binomial 模型**：
    *   **理由**：二项分布假设每个家庭生男概率 $\pi$ 都是死的（恒定）。但实际上，不同家庭的遗传背景不同，每个家庭的 $\pi$ 可能是在变化的。
    *   **数学建模**：假设 $\pi$ 本身服从一个 **Beta 分布**。
    *   **结果**：两个分布“嵌套”在一起，形成了 **Beta-Binomial 分布**。它比普通二项分布多了一个调节参数，可以更好地捕捉数据的波动。

### 3. 拟合优度检验：谁才是真理？ (Slide 31-32)
这部分是解题的关键步骤，教你如何用数据“打脸”不合适的模型。

*   **$\chi^2$ 拟合优度检验 (Goodness of Fit)**：
    *   **原理**：计算“观测值 (Observed)”与“模型预测值 (Expected)”之间的差距。
    *   **公式**：$\chi^2 = \sum \frac{(O_i - E_i)^2}{E_i}$。
*   **模型对决**：
    1.  **假设 $H_0$：数据符合二项分布**。
        *   结果：$p$ 值极小（$4.7 \times 10^{-18}$）。
        *   结论：**无情拒绝**。数据绝对不符合简单二项分布。
    2.  **假设 $H_0$：数据符合 Beta-Binomial 分布**。
        *   结果：$p = 0.271$（大于 0.05）。
        *   结论：**接受**。这个模型能完美解释数据。

---

### 4. 重点：与作业四 (RNA-seq) 的联系
这部分内容是你做**作业四第二题**的直接理论指导。

在 RNA-seq 分析中，我们经常面临同样的抉择：
*   **模型 A：Poisson 分布**。
    *   特点：均值等于方差（$\mu = \sigma^2$）。
    *   对应讲义中的：二项分布。
    *   缺陷：通常生物重复之间波动很大，导致方差远大于均值（超离散）。
*   **模型 B：负二项分布 (Negative Binomial, NB)**。
    *   特点：引入了额外参数来吸收多余的方差。
    *   对应讲义中的：Beta-Binomial 分布。

**作业四解题思路**：
1.  计算 4 组肝脏数据的均值和方差。
2.  看方差是否显著大于均值。
3.  如果方差远大于均值，你就得学讲义里的样子，下结论说：“由于存在超离散，传统的 Poisson 假设不成立（类似于拒绝二项分布），我们应该使用负二项分布（类似于接受 Beta-Binomial）。”

---

### 第四部分复习小结：
1.  **超离散 (Overdispersion)** 是生物统计中的常客，遇到它，简单的分布（Poisson, Binomial）就会失效。
2.  **$p$ 值的特殊含义**：在拟合优度检验中，我们**希望 $p$ 值大**（大于0.05），因为这代表观测数据与模型没有显著差异，即“拟合良好”。
3.  **计算逻辑**：估计参数 $\rightarrow$ 计算理论期望值 $\rightarrow$ $\chi^2$ 检验 $\rightarrow$ 根据 $p$ 值选模型。

这部分内容是统计建模从“理想”走向“现实”的关键一步。我们要继续讲解**第五部分（列联表与独立性模型）**吗？
